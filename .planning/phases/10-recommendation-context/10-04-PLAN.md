---
phase: 10-recommendation-context
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/lib/coachPayload.js
  - supabase/functions/gemini-coach/index.ts
  - tests/unit/coachPayload.test.js
  - tests/unit/gemini-instructions.test.jsx
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - WREC-01
  - WREC-02
  - WREC-03
  - WREC-04
user_setup: []
must_haves:
  truths:
    - "Plan-mode requests treat selected-week phase and target mileage as hard weekly directives, not soft background hints."
    - "A taper week with a low target mileage cannot drift into a materially higher-volume AI week without an explicit guardrail override explanation."
    - "Any allowed override is explained in normal coaching language and tied to a concrete philosophy red line or safety rule."
  artifacts:
    - path: "src/lib/coachPayload.js"
      provides: "Structured selected-week directive fields that distinguish hard constraints from generic context"
      exports: ["buildCoachPayload"]
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Prompt contract and response validation for taper/target-mileage adherence plus override explanation"
      min_lines: 120
    - path: "tests/unit/coachPayload.test.js"
      provides: "Payload contract coverage for explicit week directive fields"
      min_lines: 35
    - path: "tests/unit/gemini-instructions.test.jsx"
      provides: "Instruction regression coverage for hard selected-week constraints and override rationale"
      min_lines: 40
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "End-to-end contract test for taper-week target-mileage protection"
      min_lines: 55
  key_links:
    - from: "src/lib/coachPayload.js"
      to: "supabase/functions/gemini-coach/index.ts"
      via: "Structured recommendation directive enters plan-mode request body"
      pattern: "recommendationContext|weekDirective|targetMileageKm|trainingType"
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "generated weekly output"
      via: "Constraint-aware prompt + validation + override explanation"
      pattern: "taper|target mileage|override"
---

<objective>
Close the second phase-10 UAT gap by enforcing selected-week block intent, especially taper phase and near-target mileage, throughout Replace-with-AI generation.

Purpose: The current recommendation contract still allows the model to optimize away from the chosen week's block intent, which produced a 110 km week for a taper target of 60 km. This plan hardens the secure coach path so selected-week intent is a binding directive unless a documented guardrail forces an explained override.
Output: Payload and edge-function contracts explicitly enforce taper/target-mileage adherence, require override rationale when a red line applies, and add regression coverage for the taper/60 km failure case.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-recommendation-context/10-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-recommendation-context/10-01-PLAN.md
@.planning/phases/10-recommendation-context/10-02-PLAN.md
@src/lib/coachPayload.js
@supabase/functions/gemini-coach/index.ts
@tests/unit/coachPayload.test.js
@tests/unit/gemini-instructions.test.jsx
@tests/integration/edge-functions.spec.ts
</context>

<tasks>

<task id="10-04-01" type="auto">
  <name>Promote selected-week block intent into explicit hard plan-generation inputs</name>
  <files>src/lib/coachPayload.js, tests/unit/coachPayload.test.js</files>
  <action>Extend the plan-mode payload contract so the selected week's tactical directive is explicit and machine-checkable, not just descriptive context. Include fields that clearly encode week phase/training type, target mileage, and any enforcement metadata needed downstream to distinguish hard constraints from softer philosophy preferences. Keep backward compatibility for existing plan-mode consumers where feasible, but make the selected-week directive unmistakable in the request shape.</action>
  <verify>Add payload tests proving taper phase and target mileage are serialized as required directive fields rather than only narrative notes. Run `npm test -- --run tests/unit/coachPayload.test.js`.</verify>
  <done>The secure coach request receives structured week intent that can be enforced downstream.</done>
</task>

<task id="10-04-02" type="auto">
  <name>Enforce taper and target-mileage adherence in gemini-coach with override rationale</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/unit/gemini-instructions.test.jsx</files>
  <action>Update plan-mode prompt assembly and any response validation/sanitization so the selected week's phase and mileage target are treated as binding directives for week generation. Require the model to stay near the requested target mileage for taper and other intent-sensitive weeks, and only allow deviation when a philosophy red line or safety guardrail clearly applies. When such an override occurs, require a short coaching explanation in plain language that names the reason instead of silently drifting volume or phase intent.</action>
  <verify>Add instruction tests that assert selected-week constraints outrank philosophy preferences, taper intent is explicit, and override explanations are mandatory when deviation is allowed. Run `npm test -- --run tests/unit/gemini-instructions.test.jsx`.</verify>
  <done>The edge function no longer permits silent taper/target-mileage drift.</done>
</task>

<task id="10-04-03" type="auto">
  <name>Lock the taper-week failure case with end-to-end contract coverage</name>
  <files>tests/integration/edge-functions.spec.ts</files>
  <action>Add an integration scenario for a selected taper week with a 60 km target and prove the plan-mode contract either returns a near-target taper week or emits an explicit override explanation when a guardrail forces adjustment. The test should fail if the response can still suggest an unbounded high-volume week like 110 km without justification.</action>
  <verify>Run `npm test -- --run tests/integration/edge-functions.spec.ts tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx`.</verify>
  <done>The diagnosed taper/60 km regression is covered across payload, prompt, and edge-function layers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Plan-mode payload includes explicit selected-week directive fields for phase/type and target mileage
- [ ] `gemini-coach` treats selected-week taper/target-mileage intent as binding unless a red line or safety guardrail forces an override
- [ ] Any allowed override includes a plain-language coaching explanation
- [ ] Taper-week regression coverage fails on silent high-mileage drift
- [ ] `npm test -- --run tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx tests/integration/edge-functions.spec.ts` passes
</verification>

<success_criteria>

- All tasks completed
- Replace-with-AI respects selected block-plan intent for taper and near-target mileage
- Philosophy remains a guardrailed coaching layer rather than a silent source of volume drift
- The taper/60 km failure case is protected by automated regression coverage

</success_criteria>

<output>
After completion, create `.planning/phases/10-recommendation-context/10-04-SUMMARY.md`
</output>
