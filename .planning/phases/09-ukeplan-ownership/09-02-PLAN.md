---
phase: 09-ukeplan-ownership
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/pages/WeeklyPlanPage.jsx
  - tests/unit/trainingplan.test.jsx
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - UKE-02
user_setup: []
must_haves:
  truths:
    - "`LongTermPlanPage` frames weekly intent and targets without acting as a competing weekly generator."
    - "`LongTermPlanPage` exposes an explicit handoff into `WeeklyPlanPage` carrying week type, target mileage, and notes."
    - "The app presents a single weekly-planning ownership model across the two pages."
  artifacts:
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "Read-only intent presentation and handoff CTA to Ukeplan"
      min_lines: 60
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Regression coverage proving the page no longer owns weekly generation/apply behavior"
      min_lines: 40
  key_links:
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/pages/WeeklyPlanPage.jsx"
      via: "Navigation handoff carrying week intent"
      pattern: "Weekly Plan|Ukeplan"
---

<objective>
Remove the ownership conflict between `Treningsplan` and `Ukeplan`.

Purpose: Keep `Treningsplan` useful as upstream weekly intent while making it obvious that weekly generation and editing happen in `Ukeplan`.
Output: `LongTermPlanPage` becomes read-only weekly guidance plus CTA/handoff, and tests enforce the new ownership boundary.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-ukeplan-ownership/09-CONTEXT.md
@.planning/phases/09-ukeplan-ownership/09-RESEARCH.md
@src/pages/LongTermPlanPage.jsx
@src/pages/WeeklyPlanPage.jsx
@tests/unit/trainingplan.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Training Plan weekly AI area into read-only intent and handoff</name>
  <files>src/pages/LongTermPlanPage.jsx</files>
  <action>Replace the current week preview/apply ownership in `LongTermPlanPage` with a read-only weekly intent presentation and a clear CTA into `WeeklyPlanPage`. Preserve useful upstream data such as week type, target km, and notes, but remove the in-place weekly apply flow from this page.</action>
  <verify>Confirm the page no longer offers direct week application controls and instead surfaces a handoff action into `Ukeplan`. Run `npm test -- --run tests/unit/trainingplan.test.jsx`.</verify>
  <done>`Treningsplan` remains the source of weekly intent without competing with `Ukeplan` as a tactical planner.</done>
</task>

<task type="auto">
  <name>Task 2: Wire handoff context so Weekly Plan can show what it received</name>
  <files>src/pages/LongTermPlanPage.jsx, src/pages/WeeklyPlanPage.jsx</files>
  <action>Pass the selected week's type, target mileage, and notes into `WeeklyPlanPage` using the lightest-weight navigation/state mechanism already compatible with the app. Ensure `WeeklyPlanPage` shows that handoff context in its setup summary when present.</action>
  <verify>Add tests that confirm the handoff CTA exists and that `WeeklyPlanPage` renders the received intent context. Run `npm test -- --run tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The two pages now reinforce one ownership model instead of two parallel weekly workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite page-level regressions around the new ownership boundary</name>
  <files>tests/unit/trainingplan.test.jsx, tests/unit/weeklyplan.test.jsx</files>
  <action>Remove assertions that depend on `LongTermPlanPage` invoking `gemini-coach` or applying weekly structure. Replace them with tests for intent display, handoff CTA, and `Ukeplan` ownership of generation/editing.</action>
  <verify>Run `npm test -- --run tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx`.</verify>
  <done>Automated coverage matches the new UX contract for phase 9.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `LongTermPlanPage` no longer owns weekly apply/generation controls
- [ ] Handoff carries week intent into `WeeklyPlanPage`
- [ ] Updated tests enforce the ownership split
- [ ] `npm test -- --run tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- `Treningsplan` is read-only weekly intent
- `Ukeplan` is the only tactical weekly planning surface
- Cross-page regressions protect the ownership model

</success_criteria>

<output>
After completion, create `.planning/phases/09-ukeplan-ownership/09-02-SUMMARY.md`
</output>
