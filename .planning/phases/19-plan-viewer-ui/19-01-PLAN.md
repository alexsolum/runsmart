---
phase: 19-plan-viewer-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/components/planner/PlanViewer.jsx
  - src/components/planner/PhaseTimeline.jsx
  - src/components/planner/PlanWeekCard.jsx
  - src/components/planner/PlanDayCell.jsx
  - src/components/planner/PlanWorkoutCard.jsx
  - tests/unit/mockAppData.js
  - tests/unit/trainingplan.test.jsx
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
must_haves:
  truths:
    - "User can see a sticky color-coded phase timeline built from the hierarchical plan phases and jump to a phase section."
    - "User can browse the full plan in one chronological vertical scroll with week cards grouped under their phases."
    - "User can see each expanded week as a summary band plus seven day columns with explicit no-session states."
    - "The page lands near the first current-or-upcoming week instead of always starting at week 1."
  artifacts:
    - path: "src/components/planner/PlanViewer.jsx"
      provides: "Top-level hierarchical plan viewer that normalizes phases/weeks and manages scroll targets."
    - path: "src/components/planner/PhaseTimeline.jsx"
      provides: "Sticky clickable phase timeline derived from plan_data.phases."
    - path: "src/components/planner/PlanWeekCard.jsx"
      provides: "Week card with summary band and seven day columns."
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "Plan viewer integration on the long-term plan page."
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Regression coverage for timeline rendering, week cards, and rest-state rendering."
  key_links:
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/components/planner/PlanViewer.jsx"
      via: "hierarchicalPlan.plan.plan_data props"
      pattern: "PlanViewer"
    - from: "src/components/planner/PlanViewer.jsx"
      to: "src/components/planner/PhaseTimeline.jsx"
      via: "phases and active/current week metadata"
      pattern: "PhaseTimeline"
    - from: "src/components/planner/PlanWeekCard.jsx"
      to: "src/components/planner/PlanDayCell.jsx"
      via: "week.days map"
      pattern: "PlanDayCell"
---

<objective>
Create the read-only full-plan browsing experience on the long-term plan page: sticky phase jump navigation, continuous chronological week cards, and explicit day-state rendering.

Purpose: Deliver the structural viewer that makes the generated hierarchical plan readable and navigable before modal interactions are layered in.
Output: New plan viewer component tree, LongTermPlanPage integration, and tests covering VIEW-01 and VIEW-02.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-plan-viewer-ui/19-CONTEXT.md
@.planning/phases/19-plan-viewer-ui/19-RESEARCH.md
@.planning/phases/19-plan-viewer-ui/19-VALIDATION.md
@.planning/phases/18-hierarchical-plan-hook-layer/18-01-SUMMARY.md
@src/pages/LongTermPlanPage.jsx
@src/hooks/useHierarchicalPlan.js
@src/components/ui/ResponsiveModal.jsx
@src/components/planner/FourWeekGrid.jsx
@src/components/planner/WeekRow.jsx
@src/components/planner/WorkoutCard.jsx
@tests/unit/mockAppData.js
@tests/unit/trainingplan.test.jsx

<interfaces>
From src/hooks/useHierarchicalPlan.js:
```javascript
return {
  plan: state.plan,
  loading: state.loading,
  generating: state.generating,
  error: state.error,
  loadPlan,
  generatePlan,
  applyPatch,
  toggleWorkoutCompleted,
  moveWorkout,
  getWeek,
  getPhases,
};
```

From src/domain/planSchema.js and tests/unit/claudeCoach.schema.test.js:
```javascript
plan.plan_data.phases[] = {
  name,
  startWeek,
  endWeek,
  focus,
  weeklyHoursRange,
}

plan.plan_data.weeks[] = {
  weekNumber,
  startDate,
  endDate,
  phase,
  focus,
  targetHours,
  isRecoveryWeek,
  days: [{ date, dayOfWeek, workouts: [] }],
  summary: { totalHours, totalKm, sessions },
}

workout = {
  id,
  sport,
  type,
  name,
  description,
  durationMinutes,
  distanceKm,
  primaryZone,
  humanReadable,
  completed,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the hierarchical viewer contracts, fixtures, and layout components</name>
  <files>src/components/planner/PlanViewer.jsx, src/components/planner/PhaseTimeline.jsx, src/components/planner/PlanWeekCard.jsx, src/components/planner/PlanDayCell.jsx, src/components/planner/PlanWorkoutCard.jsx, tests/unit/mockAppData.js, tests/unit/trainingplan.test.jsx</files>
  <read_first>
src/components/planner/FourWeekGrid.jsx
src/components/planner/WeekRow.jsx
src/components/planner/WorkoutCard.jsx
tests/unit/mockAppData.js
tests/unit/trainingplan.test.jsx
src/hooks/useHierarchicalPlan.js
.planning/phases/19-plan-viewer-ui/19-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: timeline renders Base/Build/Peak/Taper labels from hierarchical phases and each phase button targets a section id derived from the phase name plus start/end week.
    - Test 2: viewer renders a week card header band containing week label, phase label, focus text, total km, and total hours.
    - Test 3: each week renders exactly seven day cells, and empty cells show explicit `Rest day` or `No session planned` copy instead of blank boxes.
    - Test 4: past phases can collapse into a compact summary strip while current and future phases stay expanded.
  </behavior>
  <action>Create a new viewer tree under `src/components/planner/` with these concrete responsibilities: `PlanViewer.jsx` accepts `{ planData, onWorkoutSelect }`, derives `phases` and `weeks`, groups weeks under their matching phase by `week.weekNumber`, computes `currentWeekNumber` as the first week whose `endDate >= today` (fallback to the first week), renders a sticky `PhaseTimeline`, and renders phase sections in chronological order. `PhaseTimeline.jsx` must render one button per phase using `phase.name`, a width/flex span based on `(endWeek - startWeek + 1)`, Precision Athlete phase colors for Base/Build/Peak/Taper, and `position: sticky` near the top of the page so the jump bar remains visible while scrolling. Clicking a phase button must call `document.getElementById(sectionId)?.scrollIntoView({ block: "start", behavior: "smooth" })`. `PlanViewer.jsx` must also auto-focus the first current-or-upcoming week exactly once after mount when that week index is greater than 1 by calling `scrollIntoView({ block: "start", behavior: "auto" })` on the matching week element. Implement collapsible past phases only: if `phase.endWeek < currentWeekNumber`, render the phase section collapsed by default as a compact strip showing phase label, week range, and date range, with a button to expand; current and future phases must always render expanded and must not offer collapse on initial load. `PlanWeekCard.jsx` must render a summary band above the grid with `Week {week.weekNumber}`, the phase badge, the full `week.focus` string, right-aligned metrics where `summary.totalKm` appears before `summary.totalHours`, and a seven-column desktop grid of day cells. `PlanDayCell.jsx` must render the day label/date, stack all workouts for that day, show `Rest day` when `workouts.length === 0` and the date is in a recovery/rest context, and otherwise show `No session planned` for empty non-rest days. `PlanWorkoutCard.jsx` must keep cards compact: show workout type, short name, one metric line preferring `distanceKm` then `durationMinutes`, plus an optional single-line `humanReadable` preview when present; do not render completion toggles on the card. Extend `tests/unit/mockAppData.js` with a realistic `hierarchicalPlan.plan.plan_data` fixture containing at least four phases, multiple weeks, empty days, one rest-style day, and workouts with `humanReadable`, `summary.totalKm`, and `summary.totalHours` so tests can assert VIEW-01 and VIEW-02 against the hierarchical shape instead of legacy `trainingBlocks` data.</action>
  <acceptance_criteria>
    - `src/components/planner/PlanViewer.jsx` exists and contains `scrollIntoView({ block: "start", behavior: "smooth" })`.
    - `src/components/planner/PhaseTimeline.jsx` exists and contains `position: "sticky"` or `className` with `sticky`.
    - `src/components/planner/PlanWeekCard.jsx` contains `Week {week.weekNumber}` and references `summary.totalKm` before `summary.totalHours`.
    - `src/components/planner/PlanDayCell.jsx` contains the exact strings `Rest day` and `No session planned`.
    - `src/components/planner/PlanWorkoutCard.jsx` does not contain `toggleWorkoutCompleted` or a checkbox/button for completion.
    - `tests/unit/mockAppData.js` contains a non-null `hierarchicalPlan.plan.plan_data`.
    - `tests/unit/trainingplan.test.jsx` contains assertions covering timeline labels, seven day cells, and explicit empty-day copy.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/trainingplan.test.jsx</automated>
  </verify>
  <done>Viewer components and fixtures exist, tests describe VIEW-01 and VIEW-02 behavior, and the hierarchical viewer can be integrated without relying on legacy weekly-planner contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the lightweight hierarchical summary on LongTermPlanPage with the full viewer</name>
  <files>src/pages/LongTermPlanPage.jsx, src/components/planner/PlanViewer.jsx, tests/unit/trainingplan.test.jsx</files>
  <read_first>
src/pages/LongTermPlanPage.jsx
src/components/planner/PlanViewer.jsx
src/hooks/useHierarchicalPlan.js
tests/unit/trainingplan.test.jsx
.planning/phases/19-plan-viewer-ui/19-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: when `hierarchicalPlan.plan` exists, LongTermPlanPage renders the new `PlanViewer` instead of the current race/date-only summary block.
    - Test 2: loading and empty-plan states still render `Loading plan...` and `No training plan yet.` respectively.
    - Test 3: the page keeps the existing `Regenerate Plan` action and passes workout-card selection callbacks down without opening the modal yet.
  </behavior>
  <action>Update `src/pages/LongTermPlanPage.jsx` so the hierarchical plan section becomes the plan viewer host instead of a two-field summary card. Keep the existing loading, error, empty-state, and regenerate CTA logic. In the loaded state, derive `planData = hierarchicalPlan.plan.plan_data`, render a section heading for the plan plus race/event metadata, then mount `<PlanViewer planData={planData} onWorkoutSelect={setSelectedWorkoutPayload} />`. Add local page state for the selected workout payload and selected viewer week/day metadata even if the modal is introduced in Plan 02, so the viewer callback contract is already in place. Remove the old race/date-only loaded-state markup once the viewer is mounted. Keep the legacy `trainingBlocks` editor and weekly handoff area below the new hierarchical viewer; this phase must not delete that older plan-management surface. Update `tests/unit/trainingplan.test.jsx` so the plan-page tests assert the presence of the new viewer content rather than only the old static summary text.</action>
  <acceptance_criteria>
    - `src/pages/LongTermPlanPage.jsx` contains `PlanViewer`.
    - `src/pages/LongTermPlanPage.jsx` still contains the exact strings `Loading plan...`, `Could not load your plan. Refresh to try again.`, `No training plan yet.`, and `Regenerate Plan`.
    - `src/pages/LongTermPlanPage.jsx` contains `onWorkoutSelect`.
    - `tests/unit/trainingplan.test.jsx` asserts viewer content on `LongTermPlanPage`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/trainingplan.test.jsx</automated>
  </verify>
  <done>LongTermPlanPage hosts the full read-only plan viewer while preserving generation/loading states and leaving the weekly handoff/training-block editor intact.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/trainingplan.test.jsx` after each task. Manually verify that the sticky phase bar remains visible during long-page scroll and that the initial view lands near the first current/upcoming week without a jarring jump.
</verification>

<success_criteria>
VIEW-01 and VIEW-02 are satisfied: the page shows a sticky clickable phase bar, continuous week-by-week browsing, week summary bands with km-first metrics, and explicit rest/no-session day states sourced only from `hierarchicalPlan.plan.plan_data`.
</success_criteria>

<output>
After completion, create `.planning/phases/19-plan-viewer-ui/19-01-SUMMARY.md`
</output>
