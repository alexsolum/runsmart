---
phase: 11-constraint-aware-weekly-plans
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - supabase/migrations/20260313_weekly_plan_day_states.sql
  - src/hooks/useWorkoutEntries.js
  - src/pages/WeeklyPlanPage.jsx
  - tests/unit/useWorkoutEntries.test.js
  - tests/unit/weeklyplan.test.jsx
  - tests/unit/mockAppData.js
autonomous: true
requirements:
  - WCON-03
user_setup: []
must_haves:
  truths:
    - "Manual add, delete, or edit operations can persist day-level protection independently of whether workouts still exist on that day."
    - "Regenerating a week with protected days no longer immediately hard-replaces the whole target range."
    - "A separate, explicit full-week override path exists for replacing protected days."
  artifacts:
    - path: "supabase/migrations/20260313_weekly_plan_day_states.sql"
      provides: "Persisted day-level protection metadata for weekly planning"
      min_lines: 40
    - path: "src/hooks/useWorkoutEntries.js"
      provides: "Protected-day-aware write semantics and overwrite-policy support"
      min_lines: 140
    - path: "src/pages/WeeklyPlanPage.jsx"
      provides: "Review-before-write flow and explicit full-week override path for protected days"
      min_lines: 160
    - path: "tests/unit/useWorkoutEntries.test.js"
      provides: "Hook-level coverage for protected-day persistence and overwrite behavior"
      min_lines: 80
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Page-level coverage for protected-day review and explicit override UX"
      min_lines: 60
  key_links:
    - from: "manual create/update/delete actions"
      to: "persisted day-level metadata"
      via: "Protected-day tracking independent of row presence"
      pattern: "protectedDay|dayState|manual edit|delete"
    - from: "generated candidate week"
      to: "final week write"
      via: "Review and explicit overwrite policy"
      pattern: "review|protectedDates|forceOverwriteDates|applyStructuredPlan"
---

<objective>
Redesign the weekly write path so manual edits are protected and regeneration becomes review-first instead of silent full replacement.

Purpose: Phase 11 cannot satisfy WCON-03 with prompt work alone because `applyStructuredPlan()` currently hard-deletes the entire generated date range. This plan introduces persisted day-level protection metadata, protected-day-aware write semantics, and a review flow in `WeeklyPlanPage` before protected days can be replaced.
Output: Manual edits persist day-level protection, default regeneration preserves protected days, and explicit full-week replacement requires a deliberate override path.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-constraint-aware-weekly-plans/11-CONTEXT.md
@.planning/phases/11-constraint-aware-weekly-plans/11-RESEARCH.md
@.planning/phases/11-constraint-aware-weekly-plans/11-VALIDATION.md
@src/hooks/useWorkoutEntries.js
@src/pages/WeeklyPlanPage.jsx
@tests/unit/weeklyplan.test.jsx
@tests/unit/mockAppData.js
</context>

<tasks>

<task id="11-03-01" type="auto">
  <name>Add persisted day-level protection metadata</name>
  <files>supabase/migrations/20260313_weekly_plan_day_states.sql, src/hooks/useWorkoutEntries.js, tests/unit/useWorkoutEntries.test.js</files>
  <action>Create the minimal persisted model needed to represent protected planning days even when the user has manually deleted all workouts from a day. Extend the weekly-entry hook so manual create, update, and delete operations mark that day as protected, while simple completion toggles do not automatically become planning protection unless the codebase already treats them as plan edits.</action>
  <verify>Add hook-level tests for protected-day persistence on manual create, update, and delete. Run `npm test -- --run tests/unit/useWorkoutEntries.test.js`.</verify>
  <done>Protected planning days can survive reloads and empty-day manual edits.</done>
</task>

<task id="11-03-02" type="auto">
  <name>Make applyStructuredPlan respect overwrite policies and protected days</name>
  <files>src/hooks/useWorkoutEntries.js, tests/unit/useWorkoutEntries.test.js</files>
  <action>Refactor `applyStructuredPlan()` so it no longer assumes unconditional full-range replacement. Introduce an explicit overwrite policy that can preserve protected days by default, allow forced replacement of specific protected dates, and keep the hook as the source of truth for safe week writes rather than scattering overwrite logic in the page component.</action>
  <verify>Add tests proving protected days are preserved by default and replaced only when forced. Run `npm test -- --run tests/unit/useWorkoutEntries.test.js`.</verify>
  <done>The core write path supports safe regeneration and explicit overwrite policies.</done>
</task>

<task id="11-03-03" type="auto">
  <name>Add review-before-write UX and full-week override in WeeklyPlanPage</name>
  <files>src/pages/WeeklyPlanPage.jsx, tests/unit/weeklyplan.test.jsx, tests/unit/mockAppData.js</files>
  <action>Change the weekly generation flow so protected days trigger a review step instead of immediate apply. Surface which days will be preserved or replaced, keep the default path safe, and provide a clearly separate full-week override action for replacing protected days when the user explicitly wants a full reset. Keep this compact and inline rather than creating a new planning route.</action>
  <verify>Add page-level tests covering protected-day review and explicit full-week override. Run `npm test -- --run tests/unit/weeklyplan.test.jsx tests/unit/useWorkoutEntries.test.js`.</verify>
  <done>The user can regenerate safely without losing manual edits, while retaining an explicit full replacement option.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Manual create/update/delete operations persist protected-day state
- [ ] `applyStructuredPlan()` preserves protected days by default
- [ ] Regeneration with protected days triggers review-before-write instead of immediate full replacement
- [ ] Explicit full-week override exists and is clearly separated from the default regeneration path
- [ ] `npm test -- --run tests/unit/useWorkoutEntries.test.js tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- Manual day edits are no longer silently overwritten by AI regeneration
- A deleted-all-workouts day can still remain protected
- Users retain a deliberate full-week override path when they actually want a full reset

</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-aware-weekly-plans/11-03-SUMMARY.md`
</output>
