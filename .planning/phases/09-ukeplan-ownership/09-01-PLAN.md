---
phase: 09-ukeplan-ownership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/WeeklyPlanPage.jsx
  - src/lib/coachPayload.js
  - src/hooks/useWorkoutEntries.js
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - UKE-01
user_setup: []
must_haves:
  truths:
    - "`WeeklyPlanPage` can generate an AI week without going through `LongTermPlanPage`."
    - "Generated workouts persist directly into `workout_entries` and immediately appear in the existing weekly grid."
    - "If the target week already contains entries, replacement requires explicit user confirmation."
  artifacts:
    - path: "src/pages/WeeklyPlanPage.jsx"
      provides: "Primary AI generation entry, week setup summary, and replace-confirm flow"
      min_lines: 80
    - path: "src/hooks/useWorkoutEntries.js"
      provides: "Bounded one-week apply path used by Ukeplan generation"
      exports: ["useWorkoutEntries"]
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Regression coverage for generation ownership and overwrite protection"
      min_lines: 40
  key_links:
    - from: "src/pages/WeeklyPlanPage.jsx"
      to: "supabase/functions/gemini-coach/index.ts"
      via: "Supabase Edge Function invoke"
      pattern: "functions\\.invoke\\(\"gemini-coach\""
    - from: "src/pages/WeeklyPlanPage.jsx"
      to: "src/hooks/useWorkoutEntries.js"
      via: "Structured plan apply call"
      pattern: "applyStructuredPlan"
---

<objective>
Make `Ukeplan` the place where weekly AI generation actually happens.

Purpose: Move the week-generation entry point into the existing day-by-day workspace without creating a second planning surface.
Output: `WeeklyPlanPage` can request a generated week, show the relevant setup context, confirm replacements when needed, and write the result directly into the visible weekly grid.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-ukeplan-ownership/09-CONTEXT.md
@.planning/phases/09-ukeplan-ownership/09-RESEARCH.md
@src/pages/WeeklyPlanPage.jsx
@src/hooks/useWorkoutEntries.js
@src/pages/CoachPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add week-intent summary and AI generation entry to Ukeplan</name>
  <files>src/pages/WeeklyPlanPage.jsx, src/lib/coachPayload.js</files>
  <action>Add a non-hero AI planning entry in `WeeklyPlanPage` near the selected week context. Show a brief setup summary for the target week, including week label and any available upstream intent fields, and make generation the first recommended action when the week is empty.</action>
  <verify>Confirm the page renders the new generation entry without displacing the existing weekly grid workflow. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>`Ukeplan` presents one clear tactical workflow with AI generation embedded into the existing page structure.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply a structured week directly into workout entries</name>
  <files>src/pages/WeeklyPlanPage.jsx, src/hooks/useWorkoutEntries.js</files>
  <action>Invoke `gemini-coach` in weekly plan mode from `WeeklyPlanPage`, normalize the returned `structured_plan`, and apply it through `workoutEntries.applyStructuredPlan`. Keep the overwrite scope limited to the selected week so generation writes directly into the visible grid.</action>
  <verify>Trigger generation in tests with mocked Supabase invoke and assert `applyStructuredPlan` receives only the target week payload. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>Generated workouts are saved into the existing grid data model with no detached review workspace.</done>
</task>

<task type="auto">
  <name>Task 3: Protect non-empty weeks with explicit replacement confirmation</name>
  <files>src/pages/WeeklyPlanPage.jsx, tests/unit/weeklyplan.test.jsx</files>
  <action>Detect when the selected week already has entries and require explicit confirmation before AI generation replaces them. The warning can be simple, but it must block overwrite until the user confirms.</action>
  <verify>Add unit tests for empty-week generation and existing-week replacement confirmation. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>Users cannot silently lose an existing week when generating from `Ukeplan`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `WeeklyPlanPage` owns the week-generation trigger
- [ ] Generated plan writes through `applyStructuredPlan`
- [ ] Existing-week overwrite is explicitly confirmed
- [ ] `npm test -- --run tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- `Ukeplan` is the direct AI weekly generation surface
- Generated sessions appear in the existing weekly grid
- Existing entries are protected before replacement

</success_criteria>

<output>
After completion, create `.planning/phases/09-ukeplan-ownership/09-01-SUMMARY.md`
</output>
