---
phase: 04-insights-coach-layer
plan: 04
type: execute
gap_closure: true
wave: 2
depends_on:
  - 04-03-PLAN.md
files_modified:
  - src/pages/InsightsPage.jsx
  - src/lib/synthesisFormatter.js
  - tests/unit/insights.test.jsx
  - tests/unit/synthesisFormatter.test.js
autonomous: true
requirements:
  - INSG-02
must_haves:
  truths:
    - "Insights synthesis callout renders sectioned coaching content in four visible sections in required order"
    - "UI never displays raw JSON wrappers even if backend responds with malformed string payload"
    - "Rendered synthesis is visibly longer and structured (multi-line section format instead of single short sentence)"
    - "Section content reflects 10-12 week framing text when provided by backend"
  artifacts:
    - path: "src/pages/InsightsPage.jsx"
      provides: "Sectioned synthesis callout renderer and loading/error-safe behavior"
      contains: "synthesis-callout"
    - path: "src/lib/synthesisFormatter.js"
      provides: "Defensive normalization/parser for synthesis payload variants"
      exports: ["normalizeInsightsSynthesis"]
    - path: "tests/unit/insights.test.jsx"
      provides: "Regression tests for no-raw-JSON rendering and section order"
    - path: "tests/unit/synthesisFormatter.test.js"
      provides: "Formatter tests for malformed wrapper stripping and fallback sections"
  key_links:
    - from: "src/pages/InsightsPage.jsx"
      to: "src/lib/synthesisFormatter.js"
      via: "normalize API response before render"
      pattern: "normalizeInsightsSynthesis"
    - from: "synthesis callout renderer"
      to: "UAT requirement"
      via: "fixed section titles and ordered output"
      pattern: "Mileage \(km\) development|Race readiness"
---

<objective>
Close INSG-02 at UI layer by rendering a robust sectioned synthesis callout and hardening client parsing so malformed wrapper strings can never surface to users.

Purpose: Even with backend improvements, frontend must remain resilient and always present readable coach synthesis in the required 4-section structure.
Output: Insights UI renders stable, longer, sectioned synthesis with regression tests that lock out raw JSON leakage.
</objective>

<context>
@.planning/phases/04-insights-coach-layer/04-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-insights-coach-layer/04-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add synthesis normalization utility with wrapper stripping</name>
  <files>src/lib/synthesisFormatter.js, tests/unit/synthesisFormatter.test.js</files>
  <action>
    Implement `normalizeInsightsSynthesis(input)` that accepts object or string variants and returns stable render model:
    {
      mileageKmDevelopment: string,
      paceIntensityDistribution: string,
      longRunProgression: string,
      raceReadiness: string
    }
    Behavior:
    1) map backend snake_case keys to frontend camelCase,
    2) strip leading/trailing JSON-like wrappers and markdown fences,
    3) reject raw blobs containing unresolved braces/quotes patterns by replacing with safe fallback section text,
    4) guarantee all four fields exist and are non-empty.
    Add unit tests for valid object payload, wrapped-string payload, malformed JSON string, and missing-key fallback.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/synthesisFormatter.test.js</automated>
  </verify>
  <done>Formatter deterministically prevents JSON wrapper leakage and always returns complete section data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render ordered 4-section synthesis callout in Insights page</name>
  <files>src/pages/InsightsPage.jsx, tests/unit/insights.test.jsx</files>
  <action>
    Update Insights synthesis callout rendering to show four section headings in fixed order:
    1. Mileage (km) development
    2. Distribution of paces/intensity
    3. Long run progression
    4. Race readiness
    Integrate `normalizeInsightsSynthesis` before render and ensure component never prints raw API text directly.
    Preserve existing loading state behavior and silent error omission policy.
    Add component tests that assert:
    - all four headings are rendered in order,
    - callout text is multi-line/sectioned,
    - raw strings like `{ "synthesis": "..." }` do not appear in DOM,
    - malformed payload still renders safe fallback section text.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/insights.test.jsx tests/unit/synthesisFormatter.test.js</automated>
  </verify>
  <done>Insights callout is always structured and readable, with regression protection against raw JSON display.</done>
</task>

</tasks>

<verification>
- [ ] Insights synthesis displays the four required sections in exact order
- [ ] No raw JSON wrapper text appears in synthesis callout under malformed responses
- [ ] Callout remains longer/sectioned rather than one short paragraph
- [ ] `npm test -- --run tests/unit/insights.test.jsx tests/unit/synthesisFormatter.test.js` passes
</verification>

<success_criteria>
1. UI output matches UAT structure requirements for INSG-02.
2. Parser/formatter hardening prevents user-visible JSON artifacts.
3. Regression tests cover malformed payload and section-order requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-insights-coach-layer/04-04-SUMMARY.md`.
</output>
