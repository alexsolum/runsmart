---
phase: 22-cleanup-deprecation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - supabase/functions/gemini-coach/config.toml
  - supabase/functions/gemini-coach/index.ts
  - supabase/functions/gemini-coach/playbook.ts
  - src/lib/instructionSnippets.js
  - tests/unit/weeklyplan.test.jsx
  - tests/unit/weeklyplan.rolling.test.jsx
  - tests/unit/gemini-instructions.test.jsx
  - tests/integration/edge-functions.spec.ts
  - tests/ai/coach.spec.ts
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03
must_haves:
  truths:
    - "No deployed or local active code path uses gemini-coach."
    - "Active tests and active docs no longer teach weekly AI generation or Gemini-specific behavior."
    - "A repo audit over active sources finds zero remaining legacy runtime references."
  artifacts:
    - path: "supabase/functions/gemini-coach"
      provides: "Legacy Gemini edge-function directory removed in full, including config.toml"
      contains: "directory absent"
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Claude-only integration coverage and deletion verification steps"
      contains: "claude-coach"
    - path: "tests/ai/coach.spec.ts"
      provides: "AI sanity coverage against the surviving coach runtime"
      contains: "claude-coach"
    - path: "README.md"
      provides: "Current setup and troubleshooting guidance without gemini-coach deployment steps"
      contains: "claude-coach"
  key_links:
    - from: "tests/integration/edge-functions.spec.ts"
      to: "supabase/functions/claude-coach/index.ts"
      via: "live edge-function contract test"
      pattern: "functions/v1/claude-coach"
    - from: "README.md"
      to: "supabase CLI cleanup command"
      via: "deployment deletion instructions"
      pattern: "supabase functions delete gemini-coach"
---

<objective>
Finish the deprecation cutover by deleting the old Gemini function and all legacy weekly-plan test/docs contracts, then prove the active codebase is Claude-only.

Purpose: Implement D-06 through D-11 by hard-deleting `gemini-coach`, removing active legacy specs, and codifying the zero-reference audit that closes Phase 22.
Output: No `gemini-coach` source remains, active docs/tests are rewritten for Claude and hierarchical plans, and the final audit command set is green.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-cleanup-deprecation/22-CONTEXT.md
@.planning/phases/22-cleanup-deprecation/22-RESEARCH.md
@.planning/phases/21-coach-chat-plan-patch/21-01-SUMMARY.md
@.planning/phases/21-coach-chat-plan-patch/21-02-SUMMARY.md
@.planning/phases/22-cleanup-deprecation/22-01-PLAN.md
@supabase/functions/gemini-coach/index.ts
@supabase/functions/gemini-coach/playbook.ts
@src/lib/instructionSnippets.js
@tests/integration/edge-functions.spec.ts
@tests/unit/weeklyplan.test.jsx
@tests/unit/weeklyplan.rolling.test.jsx
@tests/unit/gemini-instructions.test.jsx
@tests/ai/coach.spec.ts
@README.md
@CLAUDE.md

<interfaces>
From `tests/integration/edge-functions.spec.ts` current helpers:
```ts
function buildInsightsSynthesisPayload() { ... mode: 'insights_synthesis' ... }
const postFn = async (request, cfg, token, slug, data) =>
  request.post(`${cfg.supabaseUrl}/functions/v1/${slug}`, { ... });
```

From `tests/ai/coach.spec.ts` current smoke target:
```ts
test.describe('AI Coach — response sanity', () => {
  test('coaching response is non-empty and contains coaching keywords', async ({ page }) => { ... });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard-delete gemini-coach and remove dead helper modules</name>
  <files>supabase/functions/gemini-coach/config.toml, supabase/functions/gemini-coach/index.ts, supabase/functions/gemini-coach/playbook.ts, src/lib/instructionSnippets.js</files>
  <read_first>supabase/functions/gemini-coach/config.toml, supabase/functions/gemini-coach/index.ts, supabase/functions/gemini-coach/playbook.ts, src/lib/instructionSnippets.js, .planning/phases/22-cleanup-deprecation/22-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The repository no longer contains `supabase/functions/gemini-coach/`.
    - Test 2: Dead helper modules used only by Gemini-era tests are removed if no active import remains.
    - Test 3: The remaining app and tests still build after the deletion.
  </behavior>
  <action>Per D-06, D-07, and D-08, hard-delete the legacy Gemini Edge Function directory in full. Remove `supabase/functions/gemini-coach/config.toml`, `supabase/functions/gemini-coach/index.ts`, and `supabase/functions/gemini-coach/playbook.ts` by deleting the entire `supabase/functions/gemini-coach/` directory; do not leave a compatibility stub, redirect, empty placeholder, or orphaned function config behind. Audit `src/lib/instructionSnippets.js` and delete it too if its only remaining purpose is Gemini-specific instruction tests. After deletion, ensure no active source imports or filesystem references still expect those files. This task is local source cleanup only; deployed-function deletion is verified in Task 3 with Supabase CLI commands.</action>
  <acceptance_criteria>
    - `Test-Path supabase/functions/gemini-coach` returns `False`.
    - `rg -n 'instructionSnippets' src tests README.md CLAUDE.md` returns no active references, or the file remains only if a surviving Claude path imports it.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "if (Test-Path 'supabase/functions/gemini-coach') { Write-Error 'supabase/functions/gemini-coach still exists'; exit 1 }"</automated>
  </verify>
  <done>The Gemini function source is deleted with no compatibility layer, and any helper module tied only to Gemini-era behavior is gone or proven still necessary for surviving code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy unit-test contracts tied to WeeklyPlanPage and Gemini</name>
  <files>tests/unit/weeklyplan.test.jsx, tests/unit/weeklyplan.rolling.test.jsx, tests/unit/gemini-instructions.test.jsx</files>
  <read_first>tests/unit/weeklyplan.test.jsx, tests/unit/weeklyplan.rolling.test.jsx, tests/unit/gemini-instructions.test.jsx, tests/unit/trainingplan.test.jsx, tests/unit/insights.test.jsx, tests/unit/coach.test.jsx, .planning/phases/22-cleanup-deprecation/22-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Legacy `WeeklyPlanPage` unit suites are deleted or replaced with assertions about the surviving long-term plan / Claude flow.
    - Test 2: Gemini-specific instruction-contract unit tests are deleted or replaced with surviving Claude-era coverage.
    - Test 3: The remaining unit suite for active pages stays green after the legacy unit files are removed.
  </behavior>
  <action>Per D-08 through D-11, remove active unit-test contracts that preserve retired behavior. Delete `tests/unit/weeklyplan.test.jsx` and `tests/unit/weeklyplan.rolling.test.jsx` unless a narrow surviving assertion must be moved into an active file such as `tests/unit/trainingplan.test.jsx`. Delete `tests/unit/gemini-instructions.test.jsx` unless you convert it into a Claude-only contract test that reads `supabase/functions/claude-coach/index.ts`. Keep historical `.planning/` artifacts untouched per D-10. Do not mix docs cleanup or Supabase deployment work into this task; this task is only about removing dead unit-test contracts and keeping the surviving active unit surface green.</action>
  <acceptance_criteria>
    - `rg -n 'WeeklyPlanPage' tests/unit` returns no matches.
    - `rg -n 'gemini-coach' tests/unit` returns no matches.
    - `Test-Path tests/unit/weeklyplan.test.jsx` returns `False`.
    - `Test-Path tests/unit/weeklyplan.rolling.test.jsx` returns `False`.
    - `Test-Path tests/unit/gemini-instructions.test.jsx` returns `False`, or the file is replaced with a Claude-only contract test.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/coach.test.jsx tests/unit/insights.test.jsx tests/unit/trainingplan.test.jsx</automated>
  </verify>
  <done>Legacy unit-test files tied to WeeklyPlanPage and Gemini are deleted or replaced, and the surviving active unit suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite active integration/docs and prove deployed-function retirement</name>
  <files>tests/integration/edge-functions.spec.ts, tests/ai/coach.spec.ts, README.md, CLAUDE.md</files>
  <read_first>tests/integration/edge-functions.spec.ts, tests/ai/coach.spec.ts, README.md, CLAUDE.md, .planning/phases/22-cleanup-deprecation/22-RESEARCH.md</read_first>
  <action>Per D-08 through D-11, rewrite the active integration tests, AI smoke test, and current docs so they stop teaching the retired architecture. In `tests/integration/edge-functions.spec.ts`, replace all `gemini-coach` POST targets with `claude-coach`, remove any `coach-philosophy-admin` and Gemini-plan assertions that only existed for the retired architecture, keep the `insights_synthesis` contract, and add an explicit post-cleanup expectation that `functions/v1/gemini-coach` is absent or returns the expected not-found behavior. In `tests/ai/coach.spec.ts`, update comments and expectations so the smoke suite explicitly targets the Claude coaching path. In `README.md` and `CLAUDE.md`, replace Gemini function references, deployment commands, and architecture notes with Claude-only wording while preserving historical `.planning/` artifacts per D-10. Then run the final audit required by D-09 through D-11: execute `rg -n 'gemini-coach|WeeklyPlanPage|WeeklyAiCard|useCoachPhilosophy|WEEKLY_PLAN_HANDOFF_KEY' src tests supabase docs README.md CLAUDE.md` and make it return no matches outside `.planning/`. Finally, use Supabase CLI to verify deployment state with an explicit list/delete/list sequence: run `supabase functions list --project-ref <project-ref>`, delete the function with `supabase functions delete gemini-coach --project-ref <project-ref> --yes` if it still exists, and rerun `supabase functions list --project-ref <project-ref>` to prove absence. Record the exact command sequence and result in the plan summary. If CLI auth or project-ref is missing, let execute-phase surface the auth checkpoint instead of skipping the verification.</action>
  <acceptance_criteria>
    - `rg -n 'gemini-coach|WeeklyPlanPage|WeeklyAiCard|useCoachPhilosophy|WEEKLY_PLAN_HANDOFF_KEY' src tests supabase docs README.md CLAUDE.md` returns no matches.
    - `rg -n 'claude-coach' tests/integration/edge-functions.spec.ts tests/ai/coach.spec.ts README.md CLAUDE.md` returns at least 1 match in each file group.
    - `supabase functions list --project-ref <project-ref>` output does not include `gemini-coach` after cleanup.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$before = supabase functions list --project-ref &lt;project-ref&gt;; if ($before -match 'gemini-coach') { supabase functions delete gemini-coach --project-ref &lt;project-ref&gt; --yes | Out-Host }; $after = supabase functions list --project-ref &lt;project-ref&gt;; if ($after -match 'gemini-coach') { throw 'gemini-coach still deployed' }; rg -n 'gemini-coach|WeeklyPlanPage|WeeklyAiCard|useCoachPhilosophy|WEEKLY_PLAN_HANDOFF_KEY' src tests supabase docs README.md CLAUDE.md"</automated>
  </verify>
  <done>The repository audit is clean, the deployed Supabase project no longer serves gemini-coach, and the summary captures the verification evidence.</done>
</task>

</tasks>

<verification>
Run the source audit and the surviving validation layers:
- `npm run build`
- `npm test -- --run tests/unit/coach.test.jsx tests/unit/insights.test.jsx tests/unit/trainingplan.test.jsx`
- `rg -n 'gemini-coach|WeeklyPlanPage|WeeklyAiCard|useCoachPhilosophy|WEEKLY_PLAN_HANDOFF_KEY' src tests supabase docs README.md CLAUDE.md`
- `supabase functions list --project-ref <project-ref>`
</verification>

<success_criteria>
- `gemini-coach` is deleted from the repository and from the deployed Supabase function list.
- Legacy weekly-plan test suites and Gemini-specific instruction tests are deleted or replaced with Claude-era coverage.
- Active docs and audits describe one AI backend and one plan path, with zero active legacy references.
</success_criteria>

<output>
After completion, create `.planning/phases/22-cleanup-deprecation/22-02-SUMMARY.md`
</output>
