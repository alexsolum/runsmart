---
phase: 05-insights-synthesis-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - tests/unit/gemini-instructions.test.jsx
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - INSG-02

must_haves:
  truths:
    - "insights_synthesis explicitly migrates from legacy JSON-wrapper contract to a 4-section plain-text contract"
    - "Returned synthesis text always includes all four sections: Mileage Trend, Intensity Distribution, Long-Run Progression, Race Readiness"
    - "Malformed wrapper responses are sanitized server-side so JSON wrapper artifacts are not returned to the client"
    - "If synthesis cannot be normalized to the required section contract, edge function returns a deterministic sectioned fallback text"
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "insights_synthesis contract, sanitizer, section validator, and fallback synthesis behavior"
      contains: "mode === \"insights_synthesis\""
    - path: "tests/unit/gemini-instructions.test.jsx"
      provides: "Assertions that synthesis instructions require plain text and section headings"
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Regression checks that malformed wrappers do not leak to response synthesis text"
  key_links:
    - from: "insights_synthesis system instruction"
      to: "sanitize + validate block"
      via: "single mode path that enforces plain-text section contract"
      pattern: "Mileage Trend|Intensity Distribution|Long-Run Progression|Race Readiness"
    - from: "sanitizeSynthesisResponse helper"
      to: "edge response body"
      via: "response always sets synthesis from sanitized + validated value"
      pattern: "return new Response\\(JSON\\.stringify\\(\\{ synthesis"
---

<objective>
Harden the `insights_synthesis` edge-function contract so it reliably returns rich sectioned plain-text coaching without JSON wrapper leakage.

Purpose: Close the diagnosed INSG-02 gap at the source by making the server enforce output quality and format before anything reaches the UI.
Output: Updated `insights_synthesis` instruction + parser/sanitizer/validator path and regression tests for wrapper leakage.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-insights-synthesis-hardening/05-RESEARCH.md
@.planning/phases/05-insights-synthesis-hardening/05-VALIDATION.md
@.planning/debug/phase-04-insights-json-wrapper.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace insights_synthesis output contract with strict sectioned plain text</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/unit/gemini-instructions.test.jsx</files>
  <behavior>
    - `insights_synthesis` instruction explicitly replaces prior JSON-wrapper response contract with 4-section plain-text output
    - `insights_synthesis` instruction explicitly forbids JSON/markdown output and requires exactly four headings
    - Heading names are locked to: `Mileage Trend`, `Intensity Distribution`, `Long-Run Progression`, `Race Readiness`
    - Instruction text references 10-12 week interpretation horizon
    - Unit tests fail if instruction contract regresses to JSON-object requirement
  </behavior>
  <action>
    In `supabase/functions/gemini-coach/index.ts`, replace the current `insights_synthesis` instruction block with a plain-text section contract:
    1. Require no JSON and no markdown.
    2. Require the exact four headings.
    3. Require concise interpretation plus one practical recommendation per section.
    4. Keep philosophy/playbook addendum flow unchanged.

    In `tests/unit/gemini-instructions.test.jsx`, add/update assertions for:
    - plain-text/no-JSON requirement wording,
    - required heading names,
    - 10-12 week horizon wording anchor.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/gemini-instructions.test.jsx</automated>
    <automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated>
  </verify>
  <done>Instruction contract is locked to plain-text sectioned synthesis and validated by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add server-side synthesis sanitization, section validation, and deterministic fallback</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/integration/edge-functions.spec.ts</files>
  <behavior>
    - Sanitizer strips markdown fences and unwraps simple JSON-like wrappers when present
    - Sanitizer removes leading `synthesis:` key/value wrapper text
    - Section validator confirms all required headings exist before response
    - If validation fails, response uses deterministic sectioned fallback text rather than raw malformed output
    - Edge regression tests confirm wrapper artifacts do not leak in response JSON and required headings are present
  </behavior>
  <action>
    In `supabase/functions/gemini-coach/index.ts`:
    1. Add `sanitizeSynthesisResponse(raw)` helper for wrapper/fence cleanup.
    2. Add `hasRequiredSynthesisSections(text)` helper.
    3. Add deterministic `buildFallbackSynthesis(...)` helper with the four required headings.
    4. In `insights_synthesis` mode, always return `{ synthesis }` from `sanitize -> validate -> fallback` pipeline.

    In `tests/integration/edge-functions.spec.ts`, add a regression case for malformed wrapper payloads and assert returned synthesis:
    - does not include `{"synthesis"`/fenced JSON artifacts,
    - includes all required headings.
    Add/update integration assertions for valid payloads so heading contract regressions fail even when wrappers are absent.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated>
  </verify>
  <done>Edge function guarantees wrapper-safe 4-section synthesis output even when model output is malformed.</done>
</task>

</tasks>

<verification>
`insights_synthesis` returns only sectioned plain-text coaching and never leaks JSON wrapper artifacts.
Required headings are guaranteed by server validation/fallback behavior.
Regression gates enforce wrapper + heading contract in both unit and integration tests.
</verification>

<success_criteria>
1. Edge synthesis response contract enforces plain text with the four required headings.
2. Sanitization + validation pipeline prevents wrapper leakage from malformed model output.
3. Unit and integration tests fail on wrapper or heading-contract regressions.
4. Post-execution artifacts are updated: `05-VALIDATION.md` task statuses for 05-01 are set from pending to observed results.
</success_criteria>

<output>
After completion, create `.planning/phases/05-insights-synthesis-hardening/05-01-SUMMARY.md`.
</output>

<requirement_to_task_mapping>
- Requirement `INSG-02` -> Task 1 (contract migration) verified by `npm test -- --run tests/unit/gemini-instructions.test.jsx`.
- Requirement `INSG-02` -> Task 2 (server sanitizer + validator + deterministic fallback) verified by `npm run test:integration -- tests/integration/edge-functions.spec.ts`.
</requirement_to_task_mapping>

<checker_issue_traceability>
- Edge contract must migrate to 4-section plain text with deterministic fallback -> Task 1 + Task 2 -> verify: `npm test -- --run tests/unit/gemini-instructions.test.jsx`, `npm run test:integration -- tests/integration/edge-functions.spec.ts`.
- Server-side sanitizer/section validator must be explicit -> Task 2 -> verify: `npm run test:integration -- tests/integration/edge-functions.spec.ts`.
- Regression tests must explicitly enforce heading + wrapper behavior -> Task 1 + Task 2 -> verify: `npm test -- --run tests/unit/gemini-instructions.test.jsx`, `npm run test:integration -- tests/integration/edge-functions.spec.ts`.
</checker_issue_traceability>
