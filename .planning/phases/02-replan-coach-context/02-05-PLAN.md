---
phase: 02-replan-coach-context
plan: 05
type: execute
gap_closure: true
wave: 2
depends_on:
  - "02-04"
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/lib/coachPayload.js
  - src/hooks/useWorkoutEntries.js
  - tests/unit/trainingplan.test.jsx
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - RPLN-01
  - RPLN-02
  - PHIL-03
user_setup: []
must_haves:
  truths:
    - "Long-term page can trigger horizon replanning and receive week-level output through race date."
    - "Request path reuses shared refresh-first payload builder so context remains current."
    - "User can preview and explicitly apply returned weekly structure without corrupting weekly entry rendering."
  artifacts:
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "Long-horizon replan trigger plus preview/apply workflow"
      contains: "long_term_replan|replan"
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Coverage for trigger, request mode, and horizon preview behavior"
      min_lines: 40
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Coverage for persisted weekly entries from long-horizon apply flow"
      min_lines: 20
  key_links:
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/lib/coachPayload.js"
      via: "Shared refresh-first payload generation before invoke"
      pattern: "buildCoachPayload"
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/hooks/useWorkoutEntries.js"
      via: "Explicit apply flow persists selected horizon weeks"
      pattern: "save|upsert|create"
---

<objective>
Complete the Phase 2 gap closure by wiring long-horizon replanning into the long-term planning UI and persistence flow.

Purpose: The API-level horizon contract must be usable by athletes from the existing long-term workflow.
Output: Trigger, preview, and explicit apply path for week-level replan output, protected by unit tests.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replan-coach-context/02-RESEARCH.md
@.planning/phases/02-replan-coach-context/02-VERIFICATION.md
@CLAUDE.md
@src/pages/LongTermPlanPage.jsx
@src/lib/coachPayload.js
@src/hooks/useWorkoutEntries.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add long-horizon replan trigger and request lifecycle in long-term page</name>
  <files>src/pages/LongTermPlanPage.jsx</files>
  <action>Add or extend the manual replan action to invoke the new long-horizon mode, including loading/error/success states and clear race-horizon summary in UI.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Long-term planning flow requests and receives horizon output through race date.</done>
</task>

<task type="auto">
  <name>Task 2: Keep payload freshness and implement explicit apply of selected horizon weeks</name>
  <files>src/pages/LongTermPlanPage.jsx, src/lib/coachPayload.js, src/hooks/useWorkoutEntries.js</files>
  <action>Reuse `buildCoachPayload` for invocation and implement preview-first apply behavior where user confirms which returned weeks to write to workout entries. Preserve weekly UI compatibility and avoid destructive overwrite without confirmation.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Fresh context drives replanning and persisted results remain render-safe downstream.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for horizon coverage and apply safety</name>
  <files>tests/unit/trainingplan.test.jsx, tests/unit/weeklyplan.test.jsx</files>
  <action>Add tests that verify request mode selection, horizon preview contents, explicit apply behavior, and weekly rendering safety after persisted updates.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Unit tests protect the full long-horizon replan UX and persistence contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Long-term page invokes long-horizon replan mode and displays week-level output
- [ ] Request path uses shared fresh payload builder
- [ ] Apply action is explicit and preserves weekly-entry schema compatibility
- [ ] `npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- `RPLN-01` end-to-end flow satisfies horizon intent up to goal race
- `RPLN-02` freshness guarantees remain in effect for the new flow
- Weekly plan downstream surfaces continue to render persisted output safely

</success_criteria>

<output>
After completion, create `.planning/phases/02-replan-coach-context/02-05-SUMMARY.md`
</output>
