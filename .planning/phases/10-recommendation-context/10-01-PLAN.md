---
phase: 10-recommendation-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/WeeklyPlanPage.jsx
  - src/lib/coachPayload.js
  - tests/unit/weeklyplan.test.jsx
  - tests/unit/coachPayload.test.js
autonomous: true
requirements:
  - WREC-01
  - WREC-02
user_setup: []
must_haves:
  truths:
    - "`WeeklyPlanPage` resolves one authoritative selected-week intent and uses the same values for both the setup summary and AI generation payload."
    - "`buildCoachPayload` returns a distinct `recommendationContext` carrying selected-week dates, training type, target mileage, and notes instead of relying on current-block inference alone."
    - "Week-specific recommendation context remains anchored to the user-selected week, including non-current weeks handed off from `Treningsplan`."
  artifacts:
    - path: "src/pages/WeeklyPlanPage.jsx"
      provides: "Authoritative selected-week intent resolution and plan invocation payload assembly"
      min_lines: 70
    - path: "src/lib/coachPayload.js"
      provides: "Reusable recommendation-context contract returned with coach payloads"
      exports: ["buildCoachPayload"]
    - path: "tests/unit/coachPayload.test.js"
      provides: "Regression coverage for recommendation-context shape and week anchoring"
      min_lines: 40
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Regression coverage that displayed week intent is the same context sent to generation"
      min_lines: 45
  key_links:
    - from: "src/pages/WeeklyPlanPage.jsx"
      to: "src/lib/coachPayload.js"
      via: "Resolved selected-week intent passed into buildCoachPayload"
      pattern: "recommendationWeek|recommendationContext|buildCoachPayload"
    - from: "src/pages/WeeklyPlanPage.jsx"
      to: "supabase/functions/gemini-coach/index.ts"
      via: "Plan invoke body carrying selected week dates"
      pattern: "targetWeekStart|targetWeekEnd|functions\\.invoke\\(\"gemini-coach\""
---

<objective>
Make selected-week intent a first-class recommendation contract before it reaches the edge function.

Purpose: Close the frontend context gap so AI weekly generation uses the exact week type, target mileage, and notes the user sees in `Ukeplan`, including handoff-driven non-current weeks.
Output: `WeeklyPlanPage` resolves the authoritative selected-week intent once, `buildCoachPayload` returns a normalized `recommendationContext`, and unit tests lock the contract for `WREC-01` and `WREC-02`.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-recommendation-context/10-CONTEXT.md
@.planning/phases/10-recommendation-context/10-RESEARCH.md
@.planning/phases/10-recommendation-context/10-VALIDATION.md
@src/pages/WeeklyPlanPage.jsx
@src/lib/coachPayload.js
@tests/unit/weeklyplan.test.jsx
@tests/unit/coachPayload.test.js
</context>

<tasks>

<task id="10-01-01" type="auto">
  <name>Resolve one authoritative selected-week intent in WeeklyPlanPage</name>
  <files>src/pages/WeeklyPlanPage.jsx</files>
  <action>Refactor `WeeklyPlanPage` so the displayed week summary and generation flow share one resolved selected-week object. Preserve handoff precedence when the stored handoff matches `selectedPlanId` plus `planningStartDate`; otherwise derive the week intent from the matching training block. Normalize the resolved object into explicit fields for week start, week end, training type, target mileage, and notes so the page no longer relies on separate display-only and payload-only derivations.</action>
  <verify>Confirm a non-current handoff week still displays the expected training type, mileage, and notes and that the same resolved values are available for generation. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>`WeeklyPlanPage` owns one source of truth for the selected week's recommendation context.</done>
</task>

<task id="10-01-02" type="auto">
  <name>Add recommendationContext to buildCoachPayload and pass it from Ukeplan</name>
  <files>src/lib/coachPayload.js, src/pages/WeeklyPlanPage.jsx</files>
  <action>Extend `buildCoachPayload()` with an optional focused-week input such as `recommendationWeek`, then return a normalized `recommendationContext` alongside the broader `planContext`. Wire `WeeklyPlanPage` to pass the resolved selected-week intent into `buildCoachPayload()` and include the returned `recommendationContext` in the `gemini-coach` invoke body while keeping `targetWeekStart` and `targetWeekEnd` for compatibility.</action>
  <verify>Assert the generated invoke payload includes the selected week dates, training type, mileage, and notes rather than only broad `planContext`. Run `npm test -- --run tests/unit/coachPayload.test.js tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The frontend payload now distinguishes week-specific recommendation directives from generic active-plan context.</done>
</task>

<task id="10-01-03" type="auto">
  <name>Lock payload and page regressions for selected-week context</name>
  <files>tests/unit/coachPayload.test.js, tests/unit/weeklyplan.test.jsx</files>
  <action>Add focused tests that verify `buildCoachPayload()` returns `recommendationContext` with `weekStart`, `weekEnd`, `trainingType`, `targetMileageKm`, and `notes`, and that `WeeklyPlanPage` passes the same displayed week intent into generation for current and handoff-driven weeks. Keep the assertions centered on contract shape and authoritative-week selection rather than implementation internals.</action>
  <verify>Run `npm test -- --run tests/unit/coachPayload.test.js tests/unit/weeklyplan.test.jsx`.</verify>
  <done>`WREC-01` and `WREC-02` are protected by regression coverage before any edge-function prompt changes land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `WeeklyPlanPage` resolves one authoritative selected-week object for summary and generation
- [ ] `buildCoachPayload()` returns `recommendationContext` with explicit week dates, type, mileage, and notes
- [ ] `gemini-coach` invoke body includes `recommendationContext` plus compatibility week dates
- [ ] `npm test -- --run tests/unit/coachPayload.test.js tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- Selected week type from `Treningsplan` materially drives recommendation payloads
- Selected target mileage from `Treningsplan` materially drives recommendation payloads
- Page summary and AI payload stay aligned for the same week

</success_criteria>

<output>
After completion, create `.planning/phases/10-recommendation-context/10-01-SUMMARY.md`
</output>
