---
phase: 02-replan-coach-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - supabase/functions/gemini-coach/playbook.ts
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - PHIL-03
  - RPLN-02
user_setup: []
must_haves:
  truths:
    - "Plan and revision coaching modes apply the active published philosophy at runtime."
    - "Replanning remains schema-safe and does not let philosophy content override output contract or safety guardrails."
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Runtime fetch and injection of active philosophy document in plan modes"
      exports: ["Deno.serve"]
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Contract checks for philosophy-aware plan mode output"
      min_lines: 30
  key_links:
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "coach_philosophy_documents"
      via: "Published philosophy lookup per request"
      pattern: "coach_philosophy_documents"
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "validateAndSanitizePlan"
      via: "Response stays schema-safe after philosophy injection"
      pattern: "validateAndSanitizePlan"
---

<objective>
Implement runtime philosophy integration for manual replanning paths.

Purpose: Satisfy PHIL-03 for plan output generation while preserving current output safety and downstream compatibility.
Output: `gemini-coach` plan/revision modes that load active philosophy and still return validated structured weekly plans.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replan-coach-context/02-RESEARCH.md
@.planning/phases/02-replan-coach-context/02-VALIDATION.md
@CLAUDE.md
@supabase/functions/gemini-coach/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch active philosophy document at runtime</name>
  <files>supabase/functions/gemini-coach/index.ts</files>
  <action>Add a request-time helper that loads the active published global philosophy document (with graceful fallback when absent). Keep query and null-handling explicit so replanning remains available even without an active publish.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Plan/revision mode request flow has active philosophy content available before prompt assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Inject philosophy addendum with strict precedence</name>
  <files>supabase/functions/gemini-coach/index.ts, supabase/functions/gemini-coach/playbook.ts</files>
  <action>Map active philosophy sections into a compact instruction addendum and inject it into system instruction for replanning modes only. Preserve precedence: schema contract and safety/progression rules first, philosophy guidance next, static playbook fallback last.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Runtime prompt for replanning uses active philosophy without breaking existing instruction hierarchy.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration contract coverage for philosophy-aware plan output</name>
  <files>tests/integration/edge-functions.spec.ts</files>
  <action>Add or extend integration assertions that plan mode still returns schema-valid `structured_plan` and coaching text when philosophy content exists and when it is missing.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Integration tests protect philosophy runtime behavior and response-shape stability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Replanning modes fetch active philosophy at request time
- [ ] System instruction precedence is explicit and schema safety remains enforced
- [ ] Plan response remains valid structured weekly data after sanitization
- [ ] `npm run test:integration -- tests/integration/edge-functions.spec.ts` passes
</verification>

<success_criteria>

- PHIL-03 behavior is live for plan/revision execution path
- Replanning remains robust when no active philosophy document exists
- Structured weekly output contract remains downstream-safe

</success_criteria>

<output>
After completion, create `.planning/phases/02-replan-coach-context/02-01-SUMMARY.md`
</output>
