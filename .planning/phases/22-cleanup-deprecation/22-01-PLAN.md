---
phase: 22-cleanup-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/pages/InsightsPage.jsx
  - src/context/AppDataContext.jsx
  - src/lib/coachPayload.js
  - supabase/functions/claude-coach/index.ts
  - src/pages/LongTermPlanPage.jsx
  - src/lib/appNavigation.js
  - src/pages/WeeklyPlanPage.jsx
  - tests/unit/insights.test.jsx
  - tests/unit/mockAppData.js
  - tests/unit/trainingplan.test.jsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03
must_haves:
  truths:
    - "User can open Insights and receive synthesis from claude-coach instead of gemini-coach."
    - "User cannot navigate to WeeklyPlanPage from the active app shell."
    - "LongTermPlanPage no longer hands users off into a legacy weekly planner path."
  artifacts:
    - path: "src/context/AppDataContext.jsx"
      provides: "Authenticated Claude insights synthesis helper exposed through useAppData"
      contains: "invokeInsightsSynthesis"
    - path: "src/pages/InsightsPage.jsx"
      provides: "Insights page that requests synthesis through AppDataContext instead of calling Supabase directly"
      contains: "invokeInsightsSynthesis"
    - path: "supabase/functions/claude-coach/index.ts"
      provides: "Explicit insights_synthesis mode in the surviving AI backend"
      contains: "payload.mode === \"insights_synthesis\""
    - path: "src/App.jsx"
      provides: "Active navigation without WeeklyPlanPage route entry"
      contains: "training-plan"
    - path: "src/lib/appNavigation.js"
      provides: "Only surviving app navigation event constants"
      contains: "APP_NAVIGATE_EVENT"
  key_links:
    - from: "src/pages/InsightsPage.jsx"
      to: "src/context/AppDataContext.jsx"
      via: "useAppData-provided helper"
      pattern: "invokeInsightsSynthesis"
    - from: "src/context/AppDataContext.jsx"
      to: "supabase/functions/claude-coach/index.ts"
      via: "authenticated client.functions.invoke(\"claude-coach\")"
      pattern: "mode: \"insights_synthesis\""
    - from: "src/App.jsx"
      to: "src/pages/LongTermPlanPage.jsx"
      via: "NAV_GROUPS active page map"
      pattern: "component: LongTermPlanPage"
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/lib/appNavigation.js"
      via: "runtime navigation without weekly handoff storage"
      pattern: "APP_NAVIGATE_EVENT"
---

<objective>
Cut the app over to one surviving runtime path: Claude powers the remaining AI feature in Insights, and the legacy weekly planner surface is retired from active navigation and handoff flows.

Purpose: Implement D-01 through D-05 by removing the last active weekly-planner runtime path and migrating the surviving Insights synthesis call to the canonical Claude backend.
Output: Updated Claude edge-function mode handling, no active WeeklyPlanPage routing, and runtime tests that target the surviving surfaces only.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-cleanup-deprecation/22-CONTEXT.md
@.planning/phases/22-cleanup-deprecation/22-RESEARCH.md
@.planning/phases/17-claude-edge-function-plan-foundation/17-01-SUMMARY.md
@.planning/phases/19-plan-viewer-ui/19-01-SUMMARY.md
@.planning/phases/21-coach-chat-plan-patch/21-01-SUMMARY.md
@.planning/phases/21-coach-chat-plan-patch/21-02-SUMMARY.md
@src/App.jsx
@src/pages/InsightsPage.jsx
@src/context/AppDataContext.jsx
@src/pages/LongTermPlanPage.jsx
@src/pages/WeeklyPlanPage.jsx
@src/lib/appNavigation.js
@src/lib/coachPayload.js
@supabase/functions/claude-coach/index.ts
@tests/unit/insights.test.jsx
@tests/unit/mockAppData.js
@tests/unit/trainingplan.test.jsx

<interfaces>
From `src/lib/coachPayload.js`:
```javascript
const PAYLOAD_WINDOWS_BY_MODE = {
  default: { summaryWeeks: 4, activityDays: 7, logDays: 7 },
  insights_synthesis: { summaryWeeks: 12, activityDays: 84, logDays: 84 },
  chat: { summaryWeeks: 4, activityDays: 24, logDays: 7 },
};

export async function buildCoachPayload({
  activities,
  dailyLogs,
  checkins,
  activePlan,
  trainingBlocks,
  runnerProfile,
  recommendationWeek,
  weeklyConstraints,
  lang,
  mode = "default",
  hierarchicalPlanData,
})
```

From `src/lib/appNavigation.js`:
```javascript
export const APP_NAVIGATE_EVENT = "runsmart:navigate";
export const WEEKLY_PLAN_HANDOFF_KEY = "runsmart:weekly-plan-intent";
```

From `src/context/AppDataContext.jsx`:
```javascript
export function useAppData() {
  const context = useContext(AppDataContext);
  if (!context) {
    throw new Error("useAppData must be used inside AppDataProvider");
  }
  return context;
}
```

From `src/App.jsx`:
```javascript
const NAV_GROUPS = [
  {
    labelKey: "nav.general",
    items: [
      { key: "dashboard", component: HeroPage },
      { key: "training-plan", component: LongTermPlanPage },
      { key: "weekly-plan", component: WeeklyPlanPage },
      { key: "coach", component: CoachPage },
      { key: "insights", component: InsightsPage },
    ],
  },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move Insights synthesis onto claude-coach</name>
  <files>src/pages/InsightsPage.jsx, src/context/AppDataContext.jsx, src/lib/coachPayload.js, supabase/functions/claude-coach/index.ts, tests/unit/insights.test.jsx, tests/unit/mockAppData.js</files>
  <read_first>src/pages/InsightsPage.jsx, src/context/AppDataContext.jsx, src/components/chat/ChatPanel.jsx, src/lib/coachPayload.js, supabase/functions/claude-coach/index.ts, tests/unit/insights.test.jsx, tests/unit/mockAppData.js, .planning/phases/22-cleanup-deprecation/22-CONTEXT.md</read_first>
  <behavior>
    - Test 1: InsightsPage requests synthesis through a `useAppData()` helper and renders the returned `synthesis` string without invoking Supabase directly from the page.
    - Test 2: claude-coach keeps chat and generate behavior intact while adding an explicit `payload.mode === "insights_synthesis"` branch that returns `{ synthesis }`.
    - Test 3: AppDataContext owns the authenticated `claude-coach` invocation for insights synthesis, and `tests/unit/mockAppData.js` exposes the helper expected by component tests.
    - Test 4: coachPayload continues to use the 12-week / 84-day `insights_synthesis` window after the backend migration.
  </behavior>
  <action>Per D-04 and D-05, keep the Insights synthesis feature but migrate its backend from `gemini-coach` to `claude-coach` without violating the `CLAUDE.md` rule that pages use `useAppData()` rather than calling Supabase directly. Add an AppDataContext-owned helper such as `invokeInsightsSynthesis(payload)` in `src/context/AppDataContext.jsx`; implement the authenticated `client.functions.invoke("claude-coach", { body: { mode: "insights_synthesis", ...payload }, headers: { Authorization: \`Bearer ${session.access_token}\` } })` there using the same auth pattern as `src/components/chat/ChatPanel.jsx`, and expose it through `useAppData()`. In `src/pages/InsightsPage.jsx`, replace the direct `getSupabaseClient()` / `functions.invoke()` path with the new context helper so the page only builds the payload and renders state. In `supabase/functions/claude-coach/index.ts`, add a dedicated `payload.mode === "insights_synthesis"` branch before generate mode, return HTTP 200 JSON shaped as `{ synthesis, usage }`, and do not route this mode through the full-plan tool schema. Reuse `buildCoachPayload(... mode: "insights_synthesis")` unchanged apart from any exact payload fields needed by the new Claude branch. Update `tests/unit/insights.test.jsx` and `tests/unit/mockAppData.js` so the component test asserts the context helper is called and the mock app data surface matches the new AppDataContext contract.</action>
  <acceptance_criteria>
    - `rg -n 'invokeInsightsSynthesis|requestInsightsSynthesis' src/context/AppDataContext.jsx src/pages/InsightsPage.jsx tests/unit/mockAppData.js tests/unit/insights.test.jsx` returns matches showing the helper is defined, consumed, and mocked.
    - `rg -n 'functions\\.invoke\\("claude-coach"' src/context/AppDataContext.jsx` returns at least 1 match.
    - `rg -n 'functions\\.invoke\\("gemini-coach"|getSupabaseClient' src/pages/InsightsPage.jsx` returns no matches.
    - `rg -n 'payload\\.mode === "insights_synthesis"' supabase/functions/claude-coach/index.ts` returns at least 1 match.
    - `rg -n 'claude-coach|invokeInsightsSynthesis|requestInsightsSynthesis' tests/unit/insights.test.jsx` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/insights.test.jsx tests/unit/coachPayload.test.js tests/unit/claudeCoach.schema.test.js</automated>
  </verify>
  <done>Insights synthesis runs through claude-coach only, the surviving payload builder still supports `insights_synthesis`, and the targeted unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire WeeklyPlanPage and remove weekly handoff runtime wiring</name>
  <files>src/App.jsx, src/pages/LongTermPlanPage.jsx, src/lib/appNavigation.js, src/pages/WeeklyPlanPage.jsx, tests/unit/trainingplan.test.jsx</files>
  <read_first>src/App.jsx, src/pages/LongTermPlanPage.jsx, src/lib/appNavigation.js, src/pages/WeeklyPlanPage.jsx, tests/unit/trainingplan.test.jsx, .planning/phases/22-cleanup-deprecation/22-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The app shell no longer exposes a nav item with `key: "weekly-plan"` or imports `WeeklyPlanPage`.
    - Test 2: LongTermPlanPage no longer stores `WEEKLY_PLAN_HANDOFF_KEY` or dispatches navigation to the retired weekly planner.
    - Test 3: Active tests validate the surviving LongTermPlanPage behavior instead of the retired handoff path.
  </behavior>
  <action>Per D-01, D-02, and D-03, retire `WeeklyPlanPage` as an active product surface. Remove the `WeeklyPlanPage` import and `{ key: "weekly-plan", labelKey: "nav.weeklyPlan", icon: CalendarDays, component: WeeklyPlanPage }` entry from `src/App.jsx`. In `src/pages/LongTermPlanPage.jsx`, delete the session-storage handoff path that writes `WEEKLY_PLAN_HANDOFF_KEY` and the `APP_NAVIGATE_EVENT` dispatch that sends users into the weekly planner; keep LongTermPlanPage focused on the hierarchical plan viewer and its own editing/coaching actions. In `src/lib/appNavigation.js`, remove `WEEKLY_PLAN_HANDOFF_KEY` if no surviving runtime import needs it. Delete `src/pages/WeeklyPlanPage.jsx` entirely rather than leaving it orphaned. Update `tests/unit/trainingplan.test.jsx` so it no longer asserts weekly handoff storage and instead verifies the surviving LongTermPlanPage action state or navigation behavior that remains after the handoff removal.</action>
  <acceptance_criteria>
    - `rg -n 'WeeklyPlanPage' src/App.jsx src/pages src/lib tests/unit/trainingplan.test.jsx` returns no active runtime matches outside archived planning artifacts.
    - `rg -n 'WEEKLY_PLAN_HANDOFF_KEY' src tests/unit/trainingplan.test.jsx` returns no matches.
    - `Test-Path src/pages/WeeklyPlanPage.jsx` returns `False`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/trainingplan.test.jsx</automated>
  </verify>
  <done>The weekly planner page is deleted, the app shell has no weekly-planner route, and LongTermPlanPage no longer hands off into a legacy planner runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Run surviving runtime regression checks</name>
  <files>src/App.jsx, src/pages/InsightsPage.jsx, src/pages/LongTermPlanPage.jsx, tests/unit/insights.test.jsx, tests/unit/trainingplan.test.jsx</files>
  <read_first>src/App.jsx, src/pages/InsightsPage.jsx, src/pages/LongTermPlanPage.jsx, tests/unit/insights.test.jsx, tests/unit/trainingplan.test.jsx</read_first>
  <action>Run the targeted unit suite that covers the surviving app shell, Insights synthesis, and long-term plan surface after the runtime cutover. If a failure is caused by references to WeeklyPlanPage, `gemini-coach`, or `WEEKLY_PLAN_HANDOFF_KEY`, fix the production code or the active test so the surviving runtime contract is explicit and legacy-free.</action>
  <acceptance_criteria>
    - `npm test -- --run tests/unit/insights.test.jsx tests/unit/trainingplan.test.jsx tests/unit/coach.test.jsx` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/unit/insights.test.jsx tests/unit/trainingplan.test.jsx tests/unit/coach.test.jsx</automated>
  </verify>
  <done>The runtime cutover is protected by green unit tests on the surviving pages and shared Claude surfaces.</done>
</task>

</tasks>

<verification>
Run the targeted unit suite plus a source audit:
- `npm test -- --run tests/unit/insights.test.jsx tests/unit/trainingplan.test.jsx tests/unit/coach.test.jsx tests/unit/coachPayload.test.js tests/unit/claudeCoach.schema.test.js`
- `rg -n 'gemini-coach|WeeklyPlanPage|WEEKLY_PLAN_HANDOFF_KEY' src tests/unit`
</verification>

<success_criteria>
- Insights synthesis is Claude-backed with an explicit `insights_synthesis` mode in `claude-coach`.
- The app shell and long-term plan page no longer expose or navigate into the legacy weekly planner runtime.
- The deleted weekly planner path is replaced by green tests around the surviving runtime surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/22-cleanup-deprecation/22-01-SUMMARY.md`
</output>
