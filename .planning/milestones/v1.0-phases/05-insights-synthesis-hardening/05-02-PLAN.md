---
phase: 05-insights-synthesis-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/coachPayload.js
  - tests/unit/coachPayload.test.js
autonomous: true
requirements:
  - INSG-02

must_haves:
  truths:
    - "insights_synthesis payload explicitly migrates from 4-week/7-day context to a 10-12 week context contract"
    - "weeklySummary, recentActivities, and dailyLogs windows are expanded coherently for synthesis mode"
    - "payload ordering is deterministic so synthesis prompt input is stable between runs"
    - "window changes are mode-scoped and do not regress existing non-synthesis coach flows"
  artifacts:
    - path: "src/lib/coachPayload.js"
      provides: "Mode-aware payload-window sizing for insights synthesis horizon expansion"
      contains: "buildCoachPayload"
    - path: "tests/unit/coachPayload.test.js"
      provides: "Unit coverage for synthesis-specific windows and deterministic ordering"
  key_links:
    - from: "buildCoachPayload mode handling"
      to: "weekly summary/activity/log window helpers"
      via: "insights_synthesis defaults mapped to 10-12 week horizon"
      pattern: "insights_synthesis|weeks|days"
---

<objective>
Expand synthesis payload context to a 10-12 week horizon so the model has enough history to produce meaningful sectioned interpretation.

Purpose: Fix shallow output quality at the data-input layer by ensuring mileage, intensity, long-run, and readiness interpretation uses adequate historical context.
Output: Mode-aware `buildCoachPayload` windows for synthesis and tests that lock horizon + ordering behavior.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-insights-synthesis-hardening/05-RESEARCH.md
@.planning/phases/05-insights-synthesis-hardening/05-VALIDATION.md
@.planning/debug/phase-04-insights-json-wrapper.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize payload windows and add synthesis-specific defaults</name>
  <files>src/lib/coachPayload.js</files>
  <behavior>
    - `buildCoachPayload` supports mode-aware window options without breaking existing call sites
    - `insights_synthesis` uses 10-12 week weekly summaries (target default 12)
    - `insights_synthesis` extends recent-activity and daily-log windows to the same 10-12 week horizon (roughly 70-84 days)
    - Existing modes (`initial`, `followup`, `plan`, `plan_revision`, `long_term_replan`) preserve current behavior
  </behavior>
  <action>
    In `src/lib/coachPayload.js`:
    1. Add mode-aware horizon config (e.g., map by mode).
    2. Parameterize weekly/activity/log helper windows from one synthesis horizon source of truth.
    3. Use synthesis horizon defaults when mode is `insights_synthesis` and keep all three windows aligned to that horizon.
    4. Keep output shape compatible with existing edge-function request body.
    5. Keep all arrays bounded and chronologically deterministic.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/coachPayload.test.js</automated>
  </verify>
  <done>Payload builder supports synthesis horizon expansion while preserving existing mode behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock regression tests for horizon depth and ordering guarantees</name>
  <files>tests/unit/coachPayload.test.js</files>
  <behavior>
    - Synthesis-mode payload test asserts weekly summary includes >4 weeks and caps at configured horizon
    - Synthesis-mode payload test asserts activity/log windows exceed 7 days and align to the configured 10-12 week synthesis horizon
    - Ordering assertions validate predictable chronology for weeklySummary and bounded slices
    - Non-synthesis mode test asserts legacy shorter windows still apply
  </behavior>
  <action>
    In `tests/unit/coachPayload.test.js`, add focused unit tests for:
    1. `insights_synthesis` window depth (weekly summary + activities + logs).
    2. Deterministic ordering/length bounds.
    3. Backward compatibility of default mode windows.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/coachPayload.test.js</automated>
  </verify>
  <done>Tests fail on context-window regressions and protect mode-specific behavior.</done>
</task>

</tasks>

<verification>
Synthesis-mode payload includes 10-12 week context and stable ordering without changing legacy mode behavior.
</verification>

<success_criteria>
1. `buildCoachPayload` uses synthesis-specific horizon defaults aligned to 10-12 weeks.
2. Unit tests enforce depth and deterministic ordering for synthesis payload fields.
3. Existing mode payload behavior remains unchanged.
4. Post-execution artifacts are updated: `05-VALIDATION.md` task statuses for 05-02 are set from pending to observed results.
</success_criteria>

<output>
After completion, create `.planning/phases/05-insights-synthesis-hardening/05-02-SUMMARY.md`.
</output>

<requirement_to_task_mapping>
- Requirement `INSG-02` -> Task 1 (mode-aware 10-12 week payload horizon) verified by `npm test -- --run tests/unit/coachPayload.test.js`.
- Requirement `INSG-02` -> Task 2 (horizon + ordering regression guardrails) verified by `npm test -- --run tests/unit/coachPayload.test.js`.
</requirement_to_task_mapping>

<checker_issue_traceability>
- Payload horizon must be explicit 10-12 weeks and mode-aware -> Task 1 + Task 2 -> verify: `npm test -- --run tests/unit/coachPayload.test.js`.
</checker_issue_traceability>
