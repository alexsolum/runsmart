---
phase: 10-recommendation-context
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - tests/unit/gemini-instructions.test.jsx
  - tests/integration/edge-functions.spec.ts
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - WREC-01
  - WREC-02
  - WREC-03
  - WREC-04
user_setup: []
must_haves:
  truths:
    - "Plan-mode prompt assembly in `gemini-coach` uses `recommendationContext` as the primary weekly directive before philosophy preferences."
    - "The selected week dates, training type, and target mileage anchor plan generation for the chosen week instead of an implicit next-Monday fallback."
    - "Active published philosophy shapes workout choices and rationale as a background coaching layer, while explicit red-line rules can force and explain overrides."
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Secure plan-mode recommendation contract, precedence rules, and red-line override messaging"
      min_lines: 110
    - path: "tests/unit/gemini-instructions.test.jsx"
      provides: "Instruction-source regression coverage for selected-week precedence and red-line guardrails"
      min_lines: 35
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Plan-mode payload coverage proving non-current selected-week context is accepted end-to-end"
      min_lines: 45
  key_links:
    - from: "src/pages/WeeklyPlanPage.jsx"
      to: "supabase/functions/gemini-coach/index.ts"
      via: "Plan request body carrying recommendationContext"
      pattern: "recommendationContext"
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "coach_philosophy_documents"
      via: "Active published philosophy fetch and instruction addendum"
      pattern: "fetchActivePhilosophyDocument|coach_philosophy_documents"
---

<objective>
Make the secure weekly recommendation prompt follow selected-week intent first, with philosophy acting as a guardrailed coaching layer.

Purpose: Turn the new frontend recommendation contract into actual weekly behavior by anchoring plan mode to the selected week, formalizing precedence between week intent and admin philosophy, and proving the behavior through instruction and integration tests.
Output: `gemini-coach` consumes `recommendationContext`, selected-week dates replace next-Monday anchoring, philosophy red lines are explicit guardrails, and `WREC-03`/`WREC-04` are locked by automated coverage.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-recommendation-context/10-CONTEXT.md
@.planning/phases/10-recommendation-context/10-RESEARCH.md
@.planning/phases/10-recommendation-context/10-VALIDATION.md
@.planning/phases/10-recommendation-context/10-01-PLAN.md
@supabase/functions/gemini-coach/index.ts
@tests/unit/gemini-instructions.test.jsx
@tests/integration/edge-functions.spec.ts
</context>

<tasks>

<task id="10-02-01" type="auto">
  <name>Extend gemini-coach request types and plan prompt to consume recommendationContext</name>
  <files>supabase/functions/gemini-coach/index.ts</files>
  <action>Add `recommendationContext` to the plan-mode request contract and update the prompt builder so plan generation uses selected-week dates, training type, mileage, and notes before generic `planContext`. Replace implicit next-Monday wording with the request's selected week range, keep safe fallbacks for missing fields, and preserve existing structured-plan sanitization.</action>
  <verify>Confirm plan-mode prompt assembly no longer hardcodes next-Monday output when a non-current target week is provided. Run `npm test -- --run tests/unit/gemini-instructions.test.jsx tests/integration/edge-functions.spec.ts`.</verify>
  <done>Weekly generation is anchored to the requested week rather than an inferred current-week default.</done>
</task>

<task id="10-02-02" type="auto">
  <name>Tighten precedence so selected-week intent outranks philosophy preferences but not red-line guardrails</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/unit/gemini-instructions.test.jsx</files>
  <action>Update plan-mode system-instruction assembly so precedence explicitly orders output schema and safety guardrails first, then selected-week recommendation context, then active published philosophy, then playbook snippets. Make philosophy `donts` or red-line rules explicit guardrails that may force an override, and require coaching feedback to explain a meaningful override in normal coaching language.</action>
  <verify>Add instruction-source assertions for selected-week directive precedence, philosophy red-line guardrails, and required override rationale language. Run `npm test -- --run tests/unit/gemini-instructions.test.jsx`.</verify>
  <done>Published philosophy influences the week in the intended background role instead of silently overruling tactical week intent.</done>
</task>

<task id="10-02-03" type="auto">
  <name>Expand plan-mode contract tests around philosophy-backed selected-week generation</name>
  <files>tests/integration/edge-functions.spec.ts, tests/unit/weeklyplan.test.jsx</files>
  <action>Extend the integration payload builder to send `recommendationContext`, including at least one non-current selected week, and keep plan-mode contract assertions green with and without published philosophy. Add any final page-level assertions needed to prove the selected-week dates sent from `WeeklyPlanPage` match the prompt-driving payload fields expected by the edge function.</action>
  <verify>Run `npm test -- --run tests/unit/weeklyplan.test.jsx tests/integration/edge-functions.spec.ts`.</verify>
  <done>The end-to-end contract proves that selected-week context and philosophy coexist without regressing the structured weekly plan flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `gemini-coach` accepts and uses `recommendationContext` in plan mode
- [ ] Plan prompt anchors to the selected week dates instead of `getNextMonday()`
- [ ] Instruction precedence explicitly places selected-week context above philosophy preferences and red-line rules above both
- [ ] `npm test -- --run tests/unit/gemini-instructions.test.jsx tests/unit/weeklyplan.test.jsx tests/integration/edge-functions.spec.ts` passes
</verification>

<success_criteria>

- All tasks completed
- Active admin philosophy is included through the existing secure server-side path
- Weekly output visibly reflects philosophy-driven principles or red-line adjustments
- Selected week type remains primary unless an explicit guardrail forces a justified override

</success_criteria>

<output>
After completion, create `.planning/phases/10-recommendation-context/10-02-SUMMARY.md`
</output>
