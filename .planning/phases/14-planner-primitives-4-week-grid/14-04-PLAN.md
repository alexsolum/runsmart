---
phase: 14-planner-primitives-4-week-grid
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - src/components/planner/DayCell.jsx
  - tests/unit/planner-grid.test.jsx
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - GRID-05
  - GRID-06
gap_closure: true

must_haves:
  truths:
    - "User sees constraint days as dashed-border day cells while the constraint icon marker remains visible."
    - "User still sees workout/race/rest content correctly on constrained days, with constraint marker as supporting context."
    - "Planner component tests explicitly fail if dashed constraint styling regresses."
  artifacts:
    - path: "src/components/planner/DayCell.jsx"
      provides: "Constraint-aware dashed-border day cell styling with existing card/marker composition intact"
      exports: ["DayCell"]
    - path: "tests/unit/planner-grid.test.jsx"
      provides: "GRID-04 assertions for dashed constraint cell styling and coexistence behavior"
      contains: "GRID-04"
  key_links:
    - from: "src/components/planner/DayCell.jsx"
      to: "src/components/planner/ConstraintMarker.jsx"
      via: "constraintEntry conditional render path"
      pattern: "constraintEntry \\? <ConstraintMarker"
    - from: "tests/unit/planner-grid.test.jsx"
      to: "src/components/planner/DayCell.jsx"
      via: "component assertions for dashed border and marker rendering"
      pattern: "render\\(<DayCell"
---

<objective>
Close the verification gap for GRID-04 by adding dashed-border constraint cell styling in the planner day cell and locking behavior with explicit regression tests.

Purpose: Phase 14 verification failed because constraint cells currently show only icon/reason markers without dashed day-cell treatment. This plan fixes the missing visual requirement and adds focused test hardening to prevent reintroduction.

Output: Updated DayCell constraint styling and planner tests that assert dashed border + marker coexistence behavior.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-planner-primitives-4-week-grid/14-CONTEXT.md
@.planning/phases/14-planner-primitives-4-week-grid/14-RESEARCH.md
@.planning/phases/14-planner-primitives-4-week-grid/14-UI-SPEC.md
@.planning/phases/14-planner-primitives-4-week-grid/14-VERIFICATION.md
@.planning/phases/14-planner-primitives-4-week-grid/14-02-SUMMARY.md
@.planning/phases/14-planner-primitives-4-week-grid/14-03-SUMMARY.md

<interfaces>
From src/components/planner/DayCell.jsx:
```javascript
export function DayCell({ isoDate, entries = [] })
```

From src/components/planner/ConstraintMarker.jsx:
```javascript
export function ConstraintMarker({ reason })
```

From tests/unit/planner-grid.test.jsx:
```javascript
describe("DayCell", () => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashed constraint day-cell styling in DayCell</name>
  <files>src/components/planner/DayCell.jsx</files>
  <read_first>
    - src/components/planner/DayCell.jsx
    - src/components/planner/ConstraintMarker.jsx
    - .planning/phases/14-planner-primitives-4-week-grid/14-VERIFICATION.md
    - .planning/phases/14-planner-primitives-4-week-grid/14-CONTEXT.md
    - .planning/phases/14-planner-primitives-4-week-grid/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: When entries contain a constraint entry, the DayCell root element exposes dashed-border styling.
    - Test 2: When entries contain constraint + workout entries, workout cards and ConstraintMarker still render together.
    - Test 3: When entries contain no constraint, dashed-border styling is absent.
  </behavior>
  <action>Update `DayCell` to compute `const hasConstraint = Boolean(constraintEntry)` and apply a deterministic dashed-border style object on the root `<article>` when `hasConstraint` is true. Use these exact inline style keys/values in the `style` prop merge for constraint state: `borderWidth: "1px"`, `borderStyle: "dashed"`, `borderColor: "var(--pa-outline)"`. Keep existing today styling behavior intact by preserving `boxShadow: \`0 0 0 2px var(--pa-outline), var(--pa-shadow-card)\`` for `isToday`. Do not remove or move `ConstraintMarker`; keep it rendered via `{constraintEntry ? <ConstraintMarker reason={constraintEntry.description} /> : null}` after workout cards so workout cards remain visually primary when both exist. Do not modify WorkoutCard/RaceCard/RestCard routing logic.</action>
  <verify>
    <automated>npx vitest run --project components tests/unit/planner-grid.test.jsx -t "DayCell"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/planner/DayCell.jsx` contains `const hasConstraint = Boolean(constraintEntry);`
    - `src/components/planner/DayCell.jsx` contains `borderStyle: "dashed"` in the root `<article>` style merge
    - `src/components/planner/DayCell.jsx` contains `borderWidth: "1px"` in the root `<article>` style merge
    - `src/components/planner/DayCell.jsx` contains `borderColor: "var(--pa-outline)"` in the root `<article>` style merge
    - `src/components/planner/DayCell.jsx` still contains `{constraintEntry ? <ConstraintMarker reason={constraintEntry.description} /> : null}`
    - `npx vitest run --project components tests/unit/planner-grid.test.jsx -t "DayCell"` exits with code 0
  </acceptance_criteria>
  <done>Constraint day cells visibly render dashed borders without regressing existing day-card composition (workout cards + constraint marker + add affordance behavior).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden planner tests for GRID-04 and non-regression coverage</name>
  <files>tests/unit/planner-grid.test.jsx</files>
  <read_first>
    - tests/unit/planner-grid.test.jsx
    - src/components/planner/DayCell.jsx
    - .planning/phases/14-planner-primitives-4-week-grid/14-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: DayCell with constraint entry asserts dashed border on the root article style.
    - Test 2: DayCell with constraint + workout asserts both workout content and constraint marker remain rendered.
    - Test 3: DayCell without constraint asserts dashed border style is not present.
  </behavior>
  <action>In `describe("DayCell")` within `tests/unit/planner-grid.test.jsx`, replace the existing GRID-04 test that only checks the icon with explicit dashed-border assertions. Use `const { container } = render(<DayCell ... />); const cell = container.querySelector("article");` and assert `expect(cell?.getAttribute("style") ?? "").toContain("border-style: dashed")`, `...toContain("border-width: 1px")`, and `...toContain("border-color: var(--pa-outline)")` for constrained entries. Add a second test rendering constraint + workout (for example, `PLANNER_CONSTRAINT_ENTRY` + a Tempo workout) and assert both `screen.getByText("Tempo Run")` and `screen.getByLabelText("Constraint")` are present. Add a third test rendering unconstrained entries and assert the article style string does not contain `border-style: dashed`. Keep existing GRID-01/02/03/05/06 tests unchanged.</action>
  <verify>
    <automated>npx vitest run --project components tests/unit/planner-grid.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/planner-grid.test.jsx` contains a GRID-04 test assertion with `toContain("border-style: dashed")`
    - `tests/unit/planner-grid.test.jsx` contains a GRID-04 test assertion with `toContain("border-width: 1px")`
    - `tests/unit/planner-grid.test.jsx` contains a GRID-04 test assertion with `toContain("border-color: var(--pa-outline)")`
    - `tests/unit/planner-grid.test.jsx` contains a test asserting constrained day retains workout content (`Tempo Run`) and constraint icon (`Constraint`)
    - `tests/unit/planner-grid.test.jsx` contains a test asserting unconstrained day style does not include `border-style: dashed`
    - `npx vitest run --project components tests/unit/planner-grid.test.jsx` exits with code 0
  </acceptance_criteria>
  <done>Planner component tests explicitly lock dashed constraint rendering and guard against regressions in existing day-cell behavior.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project components tests/unit/planner-grid.test.jsx` passes
- `npx vitest run --project components tests/unit/weeklyplan.test.jsx` passes or only reports known pre-existing non-GRID failures documented in summary
- `npm run build` passes
</verification>

<success_criteria>
- GRID-04 verification gap is closed: constrained day cells render dashed borders plus constraint marker icon/reason
- DayCell still supports workout/race/rest rendering paths with no behavior regressions
- Planner test suite includes concrete assertions that fail if dashed constraint styling is removed
</success_criteria>

<output>
After completion, create `.planning/phases/14-planner-primitives-4-week-grid/14-04-SUMMARY.md`
</output>
