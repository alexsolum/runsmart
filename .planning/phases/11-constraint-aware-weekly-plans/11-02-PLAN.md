---
phase: 11-constraint-aware-weekly-plans
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - tests/unit/gemini-instructions.test.jsx
  - tests/unit/coachPayload.test.js
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - WCON-01
  - WCON-02
user_setup: []
must_haves:
  truths:
    - "Plan-mode generation understands structured day-level scheduling constraints in addition to selected-week directives."
    - "When a requested day preference cannot be satisfied, the response includes a short coaching explanation of the relaxed preference or moved session."
    - "Constraint reasoning remains subordinate to week-level intent and safety/philosophy guardrails, not a separate planning mode."
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Constraint-aware prompt assembly, normalization, and explanation contract for day-level weekly preferences"
      min_lines: 160
    - path: "tests/unit/gemini-instructions.test.jsx"
      provides: "Instruction-source coverage for day-level constraint precedence and explanation requirements"
      min_lines: 40
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Plan-mode contract coverage for structured weekly constraints and visible explanation output"
      min_lines: 65
  key_links:
    - from: "payload `weeklyConstraints`"
      to: "plan-mode prompt assembly"
      via: "Normalized server-side constraint contract"
      pattern: "weeklyConstraints|commuteDays|preferredLongRunDay|preferredHardWorkoutDay|doubleThresholdAllowed"
    - from: "constraint-aware prompt + validation"
      to: "response body"
      via: "Short explanation of moved sessions or relaxed preferences"
      pattern: "constraint_summary|adaptation_summary|coaching_feedback"
---

<objective>
Make the secure weekly generation path reason about day-level constraints and explain tradeoffs in visible coaching language.

Purpose: The phase-11 UI can collect constraints, but the edge function still only reasons about week-level directives and mileage. This plan teaches `gemini-coach` to consume `weeklyConstraints`, require explanation when preferences are relaxed, and lock the contract with automated tests.
Output: `gemini-coach` accepts structured weekly constraints, plan-mode responses explain moved or relaxed scheduling preferences, and regression coverage protects the new constraint-aware server contract.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-constraint-aware-weekly-plans/11-CONTEXT.md
@.planning/phases/11-constraint-aware-weekly-plans/11-RESEARCH.md
@.planning/phases/11-constraint-aware-weekly-plans/11-VALIDATION.md
@.planning/phases/11-constraint-aware-weekly-plans/11-01-PLAN.md
@supabase/functions/gemini-coach/index.ts
@src/lib/coachPayload.js
@tests/unit/gemini-instructions.test.jsx
@tests/integration/edge-functions.spec.ts
</context>

<tasks>

<task id="11-02-01" type="auto">
  <name>Extend gemini-coach request typing and prompt assembly for weeklyConstraints</name>
  <files>supabase/functions/gemini-coach/index.ts</files>
  <action>Add a normalized `weeklyConstraints` request contract to plan mode and feed it into prompt assembly alongside the existing selected-week directive. The prompt should understand preferred long-run day, preferred hard-workout day, commute days, and double-threshold allowed or forbidden, while preserving the existing precedence where selected-week intent and safety/philosophy guardrails remain authoritative.</action>
  <verify>Confirm plan-mode prompt assembly contains the structured day-level constraint fields without collapsing them into `weekDirective`. Run `npm test -- --run tests/unit/gemini-instructions.test.jsx`.</verify>
  <done>The secure coach path consumes a first-class weeklyConstraints contract.</done>
</task>

<task id="11-02-02" type="auto">
  <name>Require explanation output when constraints are relaxed or sessions move</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/unit/gemini-instructions.test.jsx</files>
  <action>Update plan-mode response requirements so the returned explanation clearly mentions moved sessions or relaxed preferences when the requested weekly constraints cannot all be satisfied. Reuse `adaptation_summary` if appropriate or add a dedicated `constraint_summary`, but the result must support the phase-11 UI decision to show one short explanation block near the week header.</action>
  <verify>Add assertions for explanation requirements tied to relaxed preferences or moved sessions. Run `npm test -- --run tests/unit/gemini-instructions.test.jsx`.</verify>
  <done>The edge function cannot silently relax weekly constraints without returning a user-facing explanation.</done>
</task>

<task id="11-02-03" type="auto">
  <name>Lock the server-side constraint contract with integration coverage</name>
  <files>tests/integration/edge-functions.spec.ts, tests/unit/coachPayload.test.js</files>
  <action>Extend the plan-mode integration payload builder and assertions so they cover `weeklyConstraints` and visible explanation output. Include at least one conflict-shaped scenario where a requested preference is likely to be relaxed, and fail the test if the response has no explanation path. Keep the test aligned with the existing secure edge-function contract style used in phase 10.</action>
  <verify>Run `npm test -- --run tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx` and `npx playwright test tests/integration/edge-functions.spec.ts`.</verify>
  <done>The constraint-aware server contract is protected by unit and integration-level regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `gemini-coach` accepts structured `weeklyConstraints` in plan mode
- [ ] Constraint conflicts produce a visible explanation path in the response contract
- [ ] Existing selected-week directive precedence remains intact
- [ ] `npm test -- --run tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx` passes
- [ ] `npx playwright test tests/integration/edge-functions.spec.ts` passes or skips only for environment setup reasons
</verification>

<success_criteria>

- All tasks completed
- Day-level constraints influence generated session placement
- Relaxed preferences or moved sessions are explained in normal coaching language
- Constraint-aware generation remains on the existing secure server-side path

</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-aware-weekly-plans/11-02-SUMMARY.md`
</output>
