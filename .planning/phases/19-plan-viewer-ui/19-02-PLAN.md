---
phase: 19-plan-viewer-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/components/planner/PlanViewer.jsx
  - src/components/planner/WorkoutDetailModal.jsx
  - src/components/planner/PlanWorkoutCard.jsx
  - src/components/planner/WorkoutForm.jsx
  - tests/unit/trainingplan.test.jsx
  - tests/unit/useHierarchicalPlan.test.jsx
autonomous: true
requirements:
  - VIEW-03
  - VIEW-04
must_haves:
  truths:
    - "User can click a workout card and open a responsive detail modal showing summary information before editing."
    - "User can toggle completion only from the workout detail modal and the grid reflects the saved state."
    - "User can edit sport, type, name, description, duration, and distance from the modal and see the updated workout card immediately."
  artifacts:
    - path: "src/components/planner/WorkoutDetailModal.jsx"
      provides: "Summary-first modal with edit mode and completion control."
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "Selected workout orchestration and hook method wiring."
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Component coverage for modal detail and edit flows."
    - path: "tests/unit/useHierarchicalPlan.test.jsx"
      provides: "Patch payload assertions for hierarchical workout edits."
  key_links:
    - from: "src/components/planner/PlanWorkoutCard.jsx"
      to: "src/components/planner/WorkoutDetailModal.jsx"
      via: "onWorkoutSelect callback"
      pattern: "onWorkoutSelect"
    - from: "src/components/planner/WorkoutDetailModal.jsx"
      to: "src/hooks/useHierarchicalPlan.js"
      via: "applyPatch and toggleWorkoutCompleted"
      pattern: "applyPatch|toggleWorkoutCompleted"
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/components/planner/WorkoutDetailModal.jsx"
      via: "selected workout state and modal open state"
      pattern: "WorkoutDetailModal"
---

<objective>
Add the workout-detail interaction layer to the full plan viewer: summary-first modal, completion toggle, and in-place workout editing backed by atomic hierarchical plan patch calls.

Purpose: Finish the editable viewer experience so workouts can be inspected and updated without leaving the page.
Output: Responsive workout detail modal, completion/edit wiring, and tests covering VIEW-03 and VIEW-04.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-plan-viewer-ui/19-CONTEXT.md
@.planning/phases/19-plan-viewer-ui/19-RESEARCH.md
@.planning/phases/19-plan-viewer-ui/19-VALIDATION.md
@.planning/phases/19-plan-viewer-ui/19-01-PLAN.md
@src/pages/LongTermPlanPage.jsx
@src/components/planner/PlanViewer.jsx
@src/components/planner/PlanWorkoutCard.jsx
@src/components/planner/WorkoutForm.jsx
@src/components/ui/ResponsiveModal.jsx
@src/hooks/useHierarchicalPlan.js
@tests/unit/trainingplan.test.jsx
@tests/unit/useHierarchicalPlan.test.jsx

<interfaces>
From src/hooks/useHierarchicalPlan.js:
```javascript
await hierarchicalPlan.toggleWorkoutCompleted(
  workoutId,
  weekNumber,
  dayDate,
);

await hierarchicalPlan.applyPatch([
  {
    week,
    dayDate,
    workoutId,
    fields: {
      sport,
      type,
      name,
      description,
      durationMinutes,
      distanceKm,
    },
  },
]);
```

From src/components/ui/ResponsiveModal.jsx:
```javascript
<ResponsiveModal open={open} onOpenChange={setOpen}>
  <ResponsiveModalContent>{children}</ResponsiveModalContent>
</ResponsiveModal>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the workout detail modal with summary-first and edit states</name>
  <files>src/components/planner/WorkoutDetailModal.jsx, src/components/planner/PlanViewer.jsx, src/components/planner/PlanWorkoutCard.jsx, src/components/planner/WorkoutForm.jsx, tests/unit/trainingplan.test.jsx</files>
  <read_first>
src/components/planner/PlanViewer.jsx
src/components/planner/PlanWorkoutCard.jsx
src/components/planner/WorkoutForm.jsx
src/components/ui/ResponsiveModal.jsx
tests/unit/trainingplan.test.jsx
.planning/phases/19-plan-viewer-ui/19-CONTEXT.md
.planning/phases/19-plan-viewer-ui/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: clicking a workout card opens a responsive modal showing workout name, sport, type, zone, duration, distance, and `humanReadable` structure in summary mode.
    - Test 2: summary mode contains `Edit workout` and a completion action, but cards themselves do not expose completion controls.
    - Test 3: switching from summary to edit resets the form for the selected workout and exposes editable fields for `sport`, `type`, `name`, `description`, `durationMinutes`, and `distanceKm`.
  </behavior>
  <action>Create `src/components/planner/WorkoutDetailModal.jsx` using `ResponsiveModal`, `ResponsiveModalContent`, `ResponsiveModalHeader`, `ResponsiveModalTitle`, and `ResponsiveModalDescription`. The component must accept `{ open, onOpenChange, selection, saving, onToggleCompleted, onSave }`, where `selection` contains `weekNumber`, `dayDate`, `dayLabel`, and the selected `workout`. Render summary mode first with the exact CTA text `Edit workout`; do not open directly into edit mode. Summary mode must show workout `name`, a badge or inline labels for `sport` and `type`, `primaryZone`, a metric row for `durationMinutes` and `distanceKm`, and the full `humanReadable` text when present. Render the completion control in the modal only, with a button label that switches between `Mark complete` and `Mark incomplete`. Then add an edit subview keyed by `${selection.workout.id}:edit` so state resets between workouts. Either extend `WorkoutForm.jsx` to support a hierarchical-plan mode or create a small embedded form inside `WorkoutDetailModal.jsx`, but the edit form must use these exact field names in the payload it returns: `sport`, `type`, `name`, `description`, `durationMinutes`, `distanceKm`. If you touch `WorkoutForm.jsx`, preserve the existing weekly-planner create/edit behavior and add a separate hierarchical mode instead of rewriting the old form contract. Update `PlanWorkoutCard.jsx` so card clicks call the viewer-level selection callback and remove any direct completion checkbox/button. Update `PlanViewer.jsx` so it passes a rich selection payload `{ phaseName, weekNumber, dayDate, dayLabel, workout }` back to the page. Extend `tests/unit/trainingplan.test.jsx` to cover opening the modal, seeing summary details, switching to edit, and confirming the card itself has no completion control.</action>
  <acceptance_criteria>
    - `src/components/planner/WorkoutDetailModal.jsx` exists and contains the exact strings `Edit workout`, `Mark complete`, and `Mark incomplete`.
    - `src/components/planner/WorkoutDetailModal.jsx` references `primaryZone` and `humanReadable`.
    - `src/components/planner/PlanWorkoutCard.jsx` contains the selection callback and no longer contains `onToggleCompleted`.
    - `src/components/planner/WorkoutForm.jsx` either contains a hierarchical mode branch or remains untouched while `WorkoutDetailModal.jsx` contains inputs named `sport`, `type`, `name`, `description`, `durationMinutes`, and `distanceKm`.
    - `tests/unit/trainingplan.test.jsx` contains assertions for modal summary mode and edit transition.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/trainingplan.test.jsx</automated>
  </verify>
  <done>The workout modal opens from viewer cards, renders summary-first content, and exposes a reset-safe edit state without reintroducing card-level completion controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire modal completion and edit saves through hierarchical plan mutations</name>
  <files>src/pages/LongTermPlanPage.jsx, src/components/planner/WorkoutDetailModal.jsx, tests/unit/trainingplan.test.jsx, tests/unit/useHierarchicalPlan.test.jsx</files>
  <read_first>
src/pages/LongTermPlanPage.jsx
src/components/planner/WorkoutDetailModal.jsx
src/hooks/useHierarchicalPlan.js
tests/unit/trainingplan.test.jsx
tests/unit/useHierarchicalPlan.test.jsx
.planning/phases/18-hierarchical-plan-hook-layer/18-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: modal completion button calls `toggleWorkoutCompleted(workoutId, weekNumber, dayDate)` and the visible workout state updates after the promise resolves.
    - Test 2: saving the edit form calls `applyPatch([{ week, dayDate, workoutId, fields }])` with the exact six editable fields and closes back to summary/grid state.
    - Test 3: the updated workout name or metrics appear in the grid without a page reload.
    - Test 4: useHierarchicalPlan tests assert the patch shape used by the viewer edit flow.
  </behavior>
  <action>Update `src/pages/LongTermPlanPage.jsx` to own the selected workout state introduced in Plan 01, render `<WorkoutDetailModal />`, and wire its callbacks to `hierarchicalPlan.toggleWorkoutCompleted` and `hierarchicalPlan.applyPatch`. The save handler must build this exact patch array shape: `[{ week: selection.weekNumber, dayDate: selection.dayDate, workoutId: selection.workout.id, fields: { sport, type, name, description, durationMinutes: NumberOrNull, distanceKm: NumberOrNull } }]`. Convert empty duration/distance inputs to `null`, not `0` or empty strings. While saving, keep the modal open and disable duplicate submissions; on success, switch the modal back to summary mode or close it, then rely on `hierarchicalPlan.plan.plan_data` state to redraw the grid with the new values. The completion handler must await `toggleWorkoutCompleted(selection.workout.id, selection.weekNumber, selection.dayDate)`. Add `tests/unit/trainingplan.test.jsx` coverage proving the modal button triggers completion, the save handler calls `applyPatch` with the exact patch fields, and the updated workout content renders in the grid. Add `tests/unit/useHierarchicalPlan.test.jsx` assertions that `applyPatch` serializes the viewer edit payload shape correctly when called with `sport`, `type`, `name`, `description`, `durationMinutes`, and `distanceKm`.</action>
  <acceptance_criteria>
    - `src/pages/LongTermPlanPage.jsx` contains `WorkoutDetailModal`.
    - `src/pages/LongTermPlanPage.jsx` contains `toggleWorkoutCompleted(selection.workout.id, selection.weekNumber, selection.dayDate)`.
    - `src/pages/LongTermPlanPage.jsx` contains `applyPatch([{` and the exact field keys `sport`, `type`, `name`, `description`, `durationMinutes`, `distanceKm`.
    - `tests/unit/trainingplan.test.jsx` asserts the patch payload shape or updated workout content after save.
    - `tests/unit/useHierarchicalPlan.test.jsx` contains an assertion covering the viewer edit patch shape.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/trainingplan.test.jsx tests/unit/useHierarchicalPlan.test.jsx</automated>
  </verify>
  <done>VIEW-03 and VIEW-04 are complete: workout cards open a summary-first modal, completion is modal-only, and edits persist through the hierarchical patch API and immediately redraw in the viewer.</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/unit/trainingplan.test.jsx` after Task 1 and `npx vitest run tests/unit/trainingplan.test.jsx tests/unit/useHierarchicalPlan.test.jsx` after Task 2. Manually verify the desktop dialog and mobile drawer both open in summary mode first, and confirm a mid-plan edit updates the visible grid without losing scroll context.
</verification>

<success_criteria>
VIEW-03 and VIEW-04 are satisfied: users open workout details from cards, toggle completion only in the modal, edit the six supported workout fields, and see the saved changes reflected in the plan viewer immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/19-plan-viewer-ui/19-02-SUMMARY.md`
</output>
