---
phase: 11-constraint-aware-weekly-plans
plan: 04
type: execute
wave: 4
depends_on:
  - 02
  - 03
files_modified:
  - tests/unit/coachPayload.test.js
  - tests/unit/gemini-instructions.test.jsx
  - tests/unit/useWorkoutEntries.test.js
  - tests/unit/weeklyplan.test.jsx
  - tests/integration/edge-functions.spec.ts
  - .planning/phases/11-constraint-aware-weekly-plans/11-VERIFICATION.md
autonomous: true
requirements:
  - WCON-01
  - WCON-02
  - WCON-03
user_setup: []
must_haves:
  truths:
    - "The final regression suite covers the new weekly constraint contract, visible explanation behavior, and protected-day overwrite semantics together."
    - "No phase-11 behavior still depends on untested prompt-only assumptions."
    - "Verification artifacts for phase 11 can point to concrete automated coverage for all three WCON requirements."
  artifacts:
    - path: "tests/unit/useWorkoutEntries.test.js"
      provides: "Stable hook-level regression coverage for protected-day semantics"
      min_lines: 100
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "End-to-end page-level regressions for constraints, explanations, review, and override behavior"
      min_lines: 80
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Secure contract coverage for weeklyConstraints and visible explanation output"
      min_lines: 75
  key_links:
    - from: "weeklyConstraints payload + edge-function response"
      to: "WeeklyPlanPage review/explanation UI"
      via: "Automated regressions that exercise both contract and user-visible behavior"
      pattern: "weeklyConstraints|constraint_summary|review|override"
---

<objective>
Harden the phase-11 verification story so the constraint-aware weekly planning flow is safe to execute and easy to verify.

Purpose: Phase 11 adds behavior across setup UI, edge-function contract, and protected-day write semantics. This plan closes the loop with the missing hook-level tests, final regression coverage, and verification evidence so the phase is not relying on scattered partial tests.
Output: Automated coverage spans payload, prompt/contract, protected-day semantics, and page behavior, and phase verification has concrete evidence for WCON-01/02/03.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-constraint-aware-weekly-plans/11-CONTEXT.md
@.planning/phases/11-constraint-aware-weekly-plans/11-RESEARCH.md
@.planning/phases/11-constraint-aware-weekly-plans/11-VALIDATION.md
@.planning/phases/11-constraint-aware-weekly-plans/11-02-PLAN.md
@.planning/phases/11-constraint-aware-weekly-plans/11-03-PLAN.md
@tests/unit/coachPayload.test.js
@tests/unit/gemini-instructions.test.jsx
@tests/unit/weeklyplan.test.jsx
@tests/integration/edge-functions.spec.ts
</context>

<tasks>

<task id="11-04-01" type="auto">
  <name>Fill the missing hook-level coverage for protected-day semantics</name>
  <files>tests/unit/useWorkoutEntries.test.js</files>
  <action>Add the dedicated hook tests identified in research and validation so protected-day persistence, preserve-by-default writes, and force-overwrite behavior are exercised outside the page layer. Keep these tests fast and deterministic so they can serve as the quick feedback loop for the phase.</action>
  <verify>Run `npm test -- --run tests/unit/useWorkoutEntries.test.js`.</verify>
  <done>Protected-day write semantics are covered at the hook level, not only through page tests.</done>
</task>

<task id="11-04-02" type="auto">
  <name>Consolidate page and server regressions around constraints and explanations</name>
  <files>tests/unit/weeklyplan.test.jsx, tests/unit/coachPayload.test.js, tests/unit/gemini-instructions.test.jsx, tests/integration/edge-functions.spec.ts</files>
  <action>Finish the cross-layer regressions so the final suite covers compact constraint entry, payload normalization, visible explanation output, protected-day review, and explicit override behavior together. Tighten any missing assertions that still leave room for silent constraint relaxation or silent overwrite.</action>
  <verify>Run `npm test -- --run tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx tests/unit/weeklyplan.test.jsx tests/unit/useWorkoutEntries.test.js` and `npx playwright test tests/integration/edge-functions.spec.ts`.</verify>
  <done>The phase has one coherent regression story across payload, edge function, hook, and page layers.</done>
</task>

<task id="11-04-03" type="auto">
  <name>Prepare verification evidence for the phase goal</name>
  <files>.planning/phases/11-constraint-aware-weekly-plans/11-VERIFICATION.md</files>
  <action>Document or pre-stage the verification evidence needed for the eventual phase verifier so WCON-01/02/03 map cleanly to the new automated coverage and any remaining manual checks. This should not fake verification early, but it should leave the final phase verification step with a clear audit trail of what was tested and why.</action>
  <verify>Confirm the verification artifact points to the actual automated commands and behavioral checks created by this phase. Use local inspection plus the executed test commands above.</verify>
  <done>The phase verifier has concrete evidence and command references instead of scattered test assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Hook-level tests exist for protected-day persistence and overwrite-policy semantics
- [ ] Unit and integration suites cover constraints, explanations, review, and override behavior together
- [ ] Verification evidence references concrete commands and behaviors for WCON-01, WCON-02, and WCON-03
- [ ] `npm test -- --run tests/unit/coachPayload.test.js tests/unit/gemini-instructions.test.jsx tests/unit/weeklyplan.test.jsx tests/unit/useWorkoutEntries.test.js` passes
- [ ] `npx playwright test tests/integration/edge-functions.spec.ts` passes or skips only for environment setup reasons
</verification>

<success_criteria>

- All tasks completed
- Phase 11 can be executed with a clear automated safety net
- Cross-layer regressions make silent overwrite or silent constraint relaxation hard to reintroduce
- Final phase verification can map directly to real test evidence

</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-aware-weekly-plans/11-04-SUMMARY.md`
</output>
