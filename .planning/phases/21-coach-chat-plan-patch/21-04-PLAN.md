---
phase: 21-coach-chat-plan-patch
plan: 04
type: gap_closure
depends_on: [21-02, 21-03]
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/components/CoachFAB.jsx
  - src/components/chat/ChatPanel.jsx
  - tests/unit/trainingplan.test.jsx
requirements: [COACH-02, COACH-03]

must_haves:
  truths:
    - "The Training Plan page exposes a coach entry point even when no hierarchical plan has been generated yet."
    - "CoachFAB can open basic coaching chat without crashing when plan context is null or incomplete."
    - "Plan-specific patch actions are only available when plan context exists, with explicit UX when plan context is missing."
    - "The Phase 21 regression is covered by tests so the FAB does not silently disappear again."
  artifacts:
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "CoachFAB mount no longer depends on hierarchicalPlan.plan existing"
    - path: "src/components/CoachFAB.jsx"
      provides: "Contextual coach entry point that tolerates empty-plan state"
    - path: "src/components/chat/ChatPanel.jsx"
      provides: "Safe behavior when plan context is null and patch actions cannot be applied"
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Regression coverage for empty-plan Training Plan state and CoachFAB visibility"
---

<objective>
Close the Phase 21 UAT gap where the CoachFAB is absent on the Training Plan page when the user has no generated hierarchical plan yet.

Purpose: preserve cross-surface coach access even in the empty-plan state, while keeping plan-patch actions safely gated until real plan context exists.
Output: CoachFAB visible in empty-plan state, safe null-plan chat behavior, regression tests, and updated UAT readiness.
</objective>

<context>
@.planning/phases/21-coach-chat-plan-patch/21-UAT.md
@.planning/debug/coachfab-missing-on-training-plan.md
@.planning/phases/21-coach-chat-plan-patch/21-02-SUMMARY.md
@.planning/phases/21-coach-chat-plan-patch/21-03-SUMMARY.md
@src/pages/LongTermPlanPage.jsx
@src/components/CoachFAB.jsx
@src/components/chat/ChatPanel.jsx
@tests/unit/trainingplan.test.jsx
</context>

<tasks>

<task>
  <name>Task 1: Remove the parent mount gate that hides CoachFAB in empty-plan state</name>
  <files>src/pages/LongTermPlanPage.jsx, src/components/CoachFAB.jsx</files>
  <action>
Change the Training Plan page so the floating coach entry point is rendered for signed-in users on the page, not only when `hierarchicalPlan.plan` exists.

Implementation constraints:
- Keep the existing empty-plan state card and Generate Plan flow intact.
- Mount `CoachFAB` with nullable plan context instead of suppressing the component entirely.
- If needed, add a prop to `CoachFAB` so it can render coach UI in "no plan yet" mode with appropriate copy.
  </action>
  <acceptance>
    - User sees CoachFAB on `/train` even when `hierarchicalPlan.plan === null`.
    - Opening the FAB does not throw or render a broken panel in empty-plan state.
  </acceptance>
</task>

<task>
  <name>Task 2: Make chat and patch UX explicit when no plan context exists</name>
  <files>src/components/CoachFAB.jsx, src/components/chat/ChatPanel.jsx</files>
  <action>
Audit the chat flow for null-plan safety.

Implementation constraints:
- Basic question/answer chat must remain possible with sparse context.
- Any plan-patch affordance must be disabled, omitted, or replaced with explicit guidance when there is no generated plan to patch.
- Do not allow hidden runtime failures caused by `hierarchicalPlan.plan`, `activePlan`, or patch-apply assumptions being null.
  </action>
  <acceptance>
    - Sending a non-patch coach question from empty-plan state works or fails with a deliberate user-facing message, never a crash.
    - Plan-specific apply flows are clearly unavailable until a plan exists.
  </acceptance>
</task>

<task>
  <name>Task 3: Add regression coverage for empty-plan CoachFAB visibility</name>
  <files>tests/unit/trainingplan.test.jsx</files>
  <action>
Add or update tests to lock in the empty-plan behavior.

Minimum coverage:
- LongTermPlanPage renders CoachFAB when no hierarchical plan is loaded.
- Opening the FAB panel in that state succeeds.
- Null-plan chat state does not expose unsafe apply behavior.
  </action>
  <acceptance>
    - Tests fail if a future change reintroduces `hierarchicalPlan.plan && <CoachFAB />` behavior.
    - Relevant unit test file passes.
  </acceptance>
</task>

<task>
  <name>Task 4: Re-run focused verification for the reopened UAT path</name>
  <files>src/pages/LongTermPlanPage.jsx, src/components/CoachFAB.jsx, src/components/chat/ChatPanel.jsx, tests/unit/trainingplan.test.jsx</files>
  <action>
After implementation, run the focused test/build checks needed to verify the gap:
- unit tests covering CoachFAB/Coach page behavior
- build or equivalent smoke check if imports or page composition changed

Record the outcome in the execution summary for the gap-closure work.
  </action>
  <acceptance>
    - The team has a clear verification path for re-running UAT tests 3 and 5 after the fix.
  </acceptance>
</task>

</tasks>
