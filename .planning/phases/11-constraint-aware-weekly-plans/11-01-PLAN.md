---
phase: 11-constraint-aware-weekly-plans
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/WeeklyPlanPage.jsx
  - src/lib/coachPayload.js
  - tests/unit/weeklyplan.test.jsx
  - tests/unit/coachPayload.test.js
autonomous: true
requirements:
  - WCON-01
user_setup: []
must_haves:
  truths:
    - "The `Ukeplan` AI setup surface exposes compact structured weekly constraints without becoming a separate heavy planning workflow."
    - "Constraint entry remains optional, but when used it serializes explicit day-level preferences for long run day, hard workout day, commute days, and double-threshold allowed or forbidden."
    - "Selected-week directive data remains separate from the new day-level constraint payload."
  artifacts:
    - path: "src/pages/WeeklyPlanPage.jsx"
      provides: "Compact structured constraint inputs inside the existing AI week setup card"
      min_lines: 120
    - path: "src/lib/coachPayload.js"
      provides: "Normalized `weeklyConstraints` payload contract distinct from `recommendationContext` and `weekDirective`"
      exports: ["buildCoachPayload"]
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Regression coverage for rendering and serializing weekly constraints from the AI setup card"
      min_lines: 70
    - path: "tests/unit/coachPayload.test.js"
      provides: "Payload contract coverage for normalized weekly constraints"
      min_lines: 45
  key_links:
    - from: "Weekly constraint controls in `WeeklyPlanPage.jsx`"
      to: "buildCoachPayload()"
      via: "Explicit `weeklyConstraints` object"
      pattern: "weeklyConstraints|preferredLongRunDay|preferredHardWorkoutDay|commuteDays|doubleThresholdAllowed"
    - from: "buildCoachPayload()"
      to: "plan-mode request body"
      via: "Separated week-level directive and day-level constraint contracts"
      pattern: "recommendationContext|weekDirective|weeklyConstraints"
---

<objective>
Establish the phase-11 frontend constraint contract before any server-side reasoning or protected-day write logic changes land.

Purpose: The app now has selected-week directive handling, but it still lacks structured day-level scheduling inputs. This plan adds compact constraint controls to the existing `Ukeplan` AI setup surface and serializes them into a normalized payload contract without turning the setup into a heavy planner form.
Output: `WeeklyPlanPage` captures optional weekly constraints, `buildCoachPayload()` normalizes them into `weeklyConstraints`, and regression tests prove the generated request includes the expected day-level preferences.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-constraint-aware-weekly-plans/11-CONTEXT.md
@.planning/phases/11-constraint-aware-weekly-plans/11-RESEARCH.md
@.planning/phases/11-constraint-aware-weekly-plans/11-VALIDATION.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/pages/WeeklyPlanPage.jsx
@src/lib/coachPayload.js
@tests/unit/weeklyplan.test.jsx
@tests/unit/coachPayload.test.js
</context>

<tasks>

<task id="11-01-01" type="auto">
  <name>Add compact structured weekly-constraint inputs to the AI setup card</name>
  <files>src/pages/WeeklyPlanPage.jsx</files>
  <action>Extend the existing `WeeklyAiCard` / `WeeklyPlanPage` setup flow with compact structured controls for preferred long-run day, preferred hard-workout day, commute days, and double-threshold allowed or forbidden. Keep the controls optional and lightweight inside the current setup surface rather than opening a separate planning screen. Preserve the selected-week setup summary and current fast path when the user does not add constraints.</action>
  <verify>Render the weekly page and confirm the new constraint controls appear inside the existing AI setup card without replacing the current selected-week setup summary. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The weekly AI setup card can collect optional day-level planning constraints inline.</done>
</task>

<task id="11-01-02" type="auto">
  <name>Normalize a distinct weeklyConstraints payload contract</name>
  <files>src/lib/coachPayload.js, src/pages/WeeklyPlanPage.jsx, tests/unit/coachPayload.test.js</files>
  <action>Extend `buildCoachPayload()` so it accepts the page's optional day-level constraint state and returns a normalized `weeklyConstraints` object. Keep `recommendationContext` and `weekDirective` intact for selected-week identity and hard week-level directives; do not fold day-level scheduling preferences into those existing structures. Wire `WeeklyPlanPage` to pass the captured constraints into `buildCoachPayload()` and the `gemini-coach` invoke body.</action>
  <verify>Add payload assertions proving `weeklyConstraints` is normalized and remains separate from `weekDirective`. Run `npm test -- --run tests/unit/coachPayload.test.js`.</verify>
  <done>The plan-generation request contains a clean, explicit weeklyConstraints contract.</done>
</task>

<task id="11-01-03" type="auto">
  <name>Lock the setup-card and payload regressions for phase-11 constraints</name>
  <files>tests/unit/weeklyplan.test.jsx, tests/unit/coachPayload.test.js</files>
  <action>Add focused tests that verify the setup card captures constraint values, quick generation still works when constraints are omitted, and the generated request includes the normalized `weeklyConstraints` fields when supplied. Keep the tests centered on contract and user-visible setup behavior rather than implementation internals.</action>
  <verify>Run `npm test -- --run tests/unit/coachPayload.test.js tests/unit/weeklyplan.test.jsx`.</verify>
  <done>WCON-01 has page-level and payload-level regression coverage before backend reasoning changes land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The AI setup surface captures optional structured weekly constraints inline
- [ ] `buildCoachPayload()` returns a distinct `weeklyConstraints` object
- [ ] `gemini-coach` invoke body includes `weeklyConstraints` when provided
- [ ] `npm test -- --run tests/unit/coachPayload.test.js tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- User can specify long-run day, hard-workout day, commute days, and double-threshold allowed or forbidden before generation
- Constraint entry stays lightweight and optional
- Day-level scheduling constraints are serialized as structured data, not free text

</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-aware-weekly-plans/11-01-SUMMARY.md`
</output>
