---
phase: 04-insights-coach-layer
plan: 03
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - src/lib/coachPayload.js
  - tests/unit/coachPayload.test.js
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - INSG-02
must_haves:
  truths:
    - "insights_synthesis returns plain text fields only and never a raw JSON wrapper string in synthesis sections"
    - "Synthesis contract is sectioned into exactly four domains: mileage_km_development, pace_intensity_distribution, long_run_progression, race_readiness"
    - "Synthesis generation uses a rolling 10-12 week evidence window from payload context"
    - "Edge fallback sanitizes malformed model output into section text values rather than echoing raw payload text"
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Sectioned insights_synthesis contract + sanitize fallback"
      contains: "insights_synthesis"
    - path: "src/lib/coachPayload.js"
      provides: "10-12 week aggregation window and synthesis context metrics"
      exports: ["buildCoachPayload"]
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Contract coverage for section keys and no-raw-JSON behavior"
    - path: "tests/unit/coachPayload.test.js"
      provides: "Window horizon coverage for 10-12 week input selection"
  key_links:
    - from: "src/lib/coachPayload.js"
      to: "supabase/functions/gemini-coach/index.ts"
      via: "mode=insights_synthesis payload carries 10-12 week summary"
      pattern: "insights_synthesis"
    - from: "insights_synthesis parser"
      to: "UI synthesis callout"
      via: "stable section keys consumed by frontend formatter"
      pattern: "mileage_km_development|race_readiness"
---

<objective>
Close the INSG-02 synthesis quality gap at contract level by replacing paragraph-only output with a sectioned 4-part synthesis schema and by extending evidence context to the last 10-12 weeks.

Purpose: Current synthesis can leak raw JSON text and is too shallow due to short history windows. A strict section contract and wider context horizon provides deterministic, richer coaching synthesis.
Output: Edge function and payload builder return reliable sectioned synthesis data with explicit sanitization and horizon tests.
</objective>

<context>
@.planning/phases/04-insights-coach-layer/04-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade insights_synthesis response contract to four required sections</name>
  <files>supabase/functions/gemini-coach/index.ts, tests/integration/edge-functions.spec.ts</files>
  <action>
    Replace freeform single-paragraph contract with explicit JSON object:
    {
      "synthesis": {
        "mileage_km_development": string,
        "pace_intensity_distribution": string,
        "long_run_progression": string,
        "race_readiness": string
      }
    }
    Update instruction prompt to require concise but richer section text (target 1-2 sentences each). Add server-side parser that:
    1) accepts compliant JSON object,
    2) strips markdown/code fences,
    3) removes wrapper artifacts like leading {"synthesis": ...} text from section values,
    4) falls back to deterministic section text defaults when parsing fails.
    Add integration tests proving section keys exist and values do not contain raw JSON wrapper markers.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated>
  </verify>
  <done>insights_synthesis edge contract is sectioned and parse fallback cannot expose raw JSON wrapper text to downstream UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand synthesis evidence horizon to rolling last 10-12 weeks</name>
  <files>src/lib/coachPayload.js, tests/unit/coachPayload.test.js</files>
  <action>
    Update payload builder context for insights synthesis mode to aggregate from latest 10-12 weeks (70-84 days) rather than short 4-week/7-day windows.
    Ensure payload includes summary inputs needed for required sections:
    - mileage progression trend in km,
    - intensity/pace distribution split,
    - long-run progression trend,
    - race-readiness signals.
    Keep existing payload compatibility for non-insights modes.
    Add unit tests that verify date-window slicing and summary metrics are based on the last 10-12 weeks only.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/coachPayload.test.js</automated>
  </verify>
  <done>Payload evidence horizon meets 10-12 week requirement and emits metrics needed by the new synthesis schema.</done>
</task>

</tasks>

<verification>
- [ ] `insights_synthesis` returns four required section keys with non-empty text
- [ ] Returned section values never include raw JSON wrapper fragments
- [ ] Payload summaries are computed from rolling 10-12 week history
- [ ] `npm run test:integration -- tests/integration/edge-functions.spec.ts` passes
- [ ] `npm test -- --run tests/unit/coachPayload.test.js` passes
</verification>

<success_criteria>
1. Backend contract for INSG-02 is sectioned, deterministic, and parser-safe.
2. 10-12 week evidence horizon is implemented and test-covered.
3. Edge fallback cannot leak raw JSON wrapper strings into synthesis fields.
</success_criteria>

<output>
After completion, create `.planning/phases/04-insights-coach-layer/04-03-SUMMARY.md`.
</output>
