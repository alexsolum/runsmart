---
phase: 02-replan-coach-context
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/pages/LongTermPlanPage.jsx
  - src/pages/WeeklyPlanPage.jsx
  - src/hooks/useWorkoutEntries.js
  - tests/unit/trainingplan.test.jsx
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - RPLN-01
  - RPLN-02
  - PHIL-03
user_setup: []
must_haves:
  truths:
    - "User can manually trigger replanning from long-term planning flow."
    - "Manual replanning request uses the shared fresh context payload and philosophy-aware edge runtime."
    - "Returned structured weekly plan is previewable and can be applied safely for downstream weekly rendering."
  artifacts:
    - path: "src/pages/LongTermPlanPage.jsx"
      provides: "Manual replan trigger, loading/error UX, and preview/apply flow"
      contains: "replan"
    - path: "src/hooks/useWorkoutEntries.js"
      provides: "Safe persistence path for applying structured weekly plan entries"
      exports: ["useWorkoutEntries"]
    - path: "tests/unit/trainingplan.test.jsx"
      provides: "Coverage for manual replan trigger and apply behavior"
      min_lines: 40
  key_links:
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/lib/coachPayload.js"
      via: "shared payload usage in replan request"
      pattern: "buildCoachPayload"
    - from: "src/pages/LongTermPlanPage.jsx"
      to: "src/hooks/useWorkoutEntries.js"
      via: "apply confirmed structured plan to weekly entries"
      pattern: "save|upsert|create"
---

<objective>
Deliver the manual long-term replanning workflow and downstream weekly application path.

Purpose: Satisfy RPLN-01 by adding user-triggered replan in long-term flow and ensuring output is usable in weekly plan UI.
Output: Replan trigger, preview-and-apply UX, and tests that validate interaction and structured data flow.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replan-coach-context/02-RESEARCH.md
@.planning/phases/02-replan-coach-context/02-VALIDATION.md
@CLAUDE.md
@src/pages/LongTermPlanPage.jsx
@src/pages/WeeklyPlanPage.jsx
@src/lib/coachPayload.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manual replan trigger and request lifecycle in long-term plan page</name>
  <files>src/pages/LongTermPlanPage.jsx</files>
  <action>Add explicit manual replan control that invokes `gemini-coach` in plan/revision path using selected plan context and shared payload builder. Include pending, success, and error states with clear user-facing copy.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Long-term page can initiate manual replanning and receive structured response payload.</done>
</task>

<task type="auto">
  <name>Task 2: Implement preview + explicit apply flow for structured weekly output</name>
  <files>src/pages/LongTermPlanPage.jsx, src/hooks/useWorkoutEntries.js, src/pages/WeeklyPlanPage.jsx</files>
  <action>Render returned structured weekly plan as preview first, then require explicit apply confirmation before persisting workout entries. Keep persisted shape aligned with weekly UI contract and existing workout entry semantics.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Manual replanning produces usable weekly output with safe explicit apply behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for trigger, payload path, and apply result</name>
  <files>tests/unit/trainingplan.test.jsx, tests/unit/weeklyplan.test.jsx</files>
  <action>Add tests validating manual replan trigger visibility/interaction, invocation using shared payload path, and successful apply behavior resulting in renderable weekly entries.</action>
  <verify><automated>npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx</automated></verify>
  <done>Unit coverage protects long-term replan workflow and downstream weekly rendering contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Long-term page exposes a manual replan trigger for selected plan flow
- [ ] Replan request path uses shared fresh payload and receives structured weekly output
- [ ] Apply action persists schema-valid workout entries after explicit confirmation
- [ ] `npm run test -- tests/unit/trainingplan.test.jsx tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- RPLN-01 manual replan trigger and flow is complete
- Replan output is safely consumable by weekly planning UI
- Phase goal is met with philosophy-aware and context-rich replanning behavior

</success_criteria>

<output>
After completion, create `.planning/phases/02-replan-coach-context/02-03-SUMMARY.md`
</output>
