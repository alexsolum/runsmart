---
phase: 05-insights-synthesis-hardening
plan: "03"
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/pages/InsightsPage.jsx
  - tests/unit/insights.test.jsx
autonomous: true
requirements:
  - INSG-02

must_haves:
  truths:
    - "InsightsPage applies explicit final UI-side synthesis sanitization/section guard immediately before render"
    - "Rendered synthesis callout never displays JSON wrapper artifacts even if malformed text reaches client"
    - "Rendered synthesis includes all four required headings when synthesis data is valid"
    - "Current UX contract is preserved: single-shot fetch, loading skeleton, and silent omission on hard failure"
  artifacts:
    - path: "src/pages/InsightsPage.jsx"
      provides: "Final render guard/sanitization and synthesis callout rendering behavior"
      contains: "data-testid=\"synthesis-callout\""
    - path: "tests/unit/insights.test.jsx"
      provides: "UI regression tests for wrapper stripping, heading presence, and failure omission"
  key_links:
    - from: "InsightsPage synthesis fetch handler"
      to: "sanitizeSynthesisText helper"
      via: "setSynthesis called only with sanitized non-empty text"
      pattern: "sanitizeSynthesisText"
    - from: "synthesis callout JSX"
      to: "synthesis state"
      via: "conditional render path for valid synthesis only"
      pattern: "synthesis-callout"
---

<objective>
Finalize client-side hardening so the Insights synthesis callout remains wrapper-safe and section-complete at render time, with regression tests proving behavior.

Purpose: Complete INSG-02 defense-in-depth by ensuring UI never leaks malformed response text and still preserves existing loading/failure UX semantics.
Output: `InsightsPage` final sanitizer guard and expanded `insights.test.jsx` coverage for wrapper leakage and section contract rendering.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-insights-synthesis-hardening/05-RESEARCH.md
@.planning/phases/05-insights-synthesis-hardening/05-VALIDATION.md
@.planning/debug/phase-04-insights-json-wrapper.md
@.planning/phases/05-insights-synthesis-hardening/05-01-SUMMARY.md
@.planning/phases/05-insights-synthesis-hardening/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final UI sanitization before setting synthesis state</name>
  <files>src/pages/InsightsPage.jsx</files>
  <behavior>
    - UI sanitizer strips residual JSON-like wrapper/fence artifacts if present
    - Sanitizer preserves valid plain-text sectioned synthesis content
    - `setSynthesis` only receives sanitized non-empty text that still satisfies the required heading contract
    - Existing one-shot fetch guard (`useRef`) and loading/failure behavior remain unchanged
  </behavior>
  <action>
    In `src/pages/InsightsPage.jsx`:
    1. Introduce local `sanitizeSynthesisText(input)` helper for final defensive cleanup.
    2. Apply helper in synthesis invoke success path before state update.
    3. Apply a final heading-presence guard before render and skip rendering when sanitized result is empty/invalid.
    4. Do not alter existing fetch trigger semantics or visible error behavior.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/insights.test.jsx</automated>
  </verify>
  <done>InsightsPage state/update path is wrapper-safe and behavior-compatible with existing UX flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Insights callout regressions for wrapper leakage and section contract</name>
  <files>tests/unit/insights.test.jsx</files>
  <behavior>
    - Test fails if rendered callout contains raw `{"synthesis"`/markdown-fence artifacts
    - Test asserts all four required section headings render for valid synthesis
    - Test confirms callout is omitted when synthesis is invalid after sanitization
    - Existing skeleton/loading tests and silent-error omission behavior remain green
  </behavior>
  <action>
    In `tests/unit/insights.test.jsx`, add/extend synthesis tests:
    1. malformed wrapper response -> rendered text is cleaned and wrapper tokens absent.
    2. valid sectioned response -> all four headings present in callout.
    3. invalid/empty sanitized response -> no synthesis callout rendered.
    4. keep existing loading and failure omission assertions intact.
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/insights.test.jsx</automated>
  </verify>
  <done>Insights page test suite protects against wrapper leakage and section-regression failures.</done>
</task>

</tasks>

<verification>
Run targeted and full suite checks:
- `npm test -- --run tests/unit/insights.test.jsx`
- `npm run test:integration -- tests/integration/edge-functions.spec.ts`
- `npm test -- --run`

Confirm synthesis callout is wrapper-safe and section-complete when data exists, and omitted on invalid/error cases, while edge integration regressions for heading/wrapper contract remain green.
</verification>

<success_criteria>
1. InsightsPage performs final sanitize-before-render guard for synthesis text.
2. Unit tests prove no JSON wrapper artifacts can appear in the rendered callout.
3. Required four-section contract is visible in successful callout rendering.
4. Unit + integration regression checks protect heading/wrapper contract across UI and edge layers.
5. Existing loading and silent-failure UX behavior remains unchanged.
6. Post-execution artifacts are updated: `05-VALIDATION.md` statuses for 05-03 are set from pending to observed results and `.planning/REQUIREMENTS.md` marks `INSG-02` complete if all phase checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-insights-synthesis-hardening/05-03-SUMMARY.md`.
</output>

<requirement_to_task_mapping>
- Requirement `INSG-02` -> Task 1 (required final UI sanitization + heading guard before render) verified by `npm test -- --run tests/unit/insights.test.jsx`.
- Requirement `INSG-02` -> Task 2 (UI regression tests for wrapper + headings + omission) verified by `npm test -- --run tests/unit/insights.test.jsx`.
</requirement_to_task_mapping>

<checker_issue_traceability>
- UI final sanitization guard must be explicit and required before render -> Task 1 -> verify: `npm test -- --run tests/unit/insights.test.jsx`.
- Regression tests must explicitly enforce heading + wrapper behavior -> Task 2 -> verify: `npm test -- --run tests/unit/insights.test.jsx`.
</checker_issue_traceability>
