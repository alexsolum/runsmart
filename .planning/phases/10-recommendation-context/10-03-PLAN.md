---
phase: 10-recommendation-context
plan: 03
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/pages/WeeklyPlanPage.jsx
  - tests/unit/weeklyplan.test.jsx
autonomous: true
requirements:
  - WREC-01
  - WREC-02
user_setup: []
must_haves:
  truths:
    - "Every visible week in `Ukeplan` exposes an AI planning path or explicit week-selection state, not only the first week in the rendered window."
    - "The selected planning week inside `WeeklyPlanPage` is the same week used for the AI setup summary and the Replace-with-AI action."
    - "Navigating within the 4-week window no longer silently falls back to `visibleWeeks[0]` for AI week setup or generation."
  artifacts:
    - path: "src/pages/WeeklyPlanPage.jsx"
      provides: "Per-week AI planning selection and selected-week-driven setup/generation flow"
      min_lines: 80
    - path: "tests/unit/weeklyplan.test.jsx"
      provides: "Regression coverage for AI planning from non-first visible weeks"
      min_lines: 55
  key_links:
    - from: "Weekly week cards or selected-week controls"
      to: "AI week setup summary"
      via: "One authoritative selected week in the 4-week window"
      pattern: "selectedWeek|visibleWeeks|Replace with AI week"
    - from: "AI setup summary"
      to: "Replace-with-AI invoke flow"
      via: "Same selected week drives UI and generation"
      pattern: "recommendationContext|targetWeekStart|targetWeekEnd"
---

<objective>
Close the first phase-10 UAT gap by making AI weekly planning available for any week the user can currently see in `Ukeplan`.

Purpose: The current flow still behaves as if only the first rendered week is plannable, which breaks the selected-week mental model and makes the AI setup card disappear for other visible weeks. This plan makes week selection explicit and ensures the setup summary and generation action always follow that selection.
Output: `WeeklyPlanPage` supports AI planning from non-first visible weeks and regression tests prove the same selected week drives both the UI summary and the generation request.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-recommendation-context/10-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-recommendation-context/10-01-PLAN.md
@src/pages/WeeklyPlanPage.jsx
@tests/unit/weeklyplan.test.jsx
</context>

<tasks>

<task id="10-03-01" type="auto">
  <name>Make week selection inside the visible 4-week window drive AI setup availability</name>
  <files>src/pages/WeeklyPlanPage.jsx</files>
  <action>Refactor the `Ukeplan` week-selection state so AI setup is anchored to the currently selected visible week rather than the first week in the rendered window. If the page already has a selected-week concept, unify AI setup availability around it; otherwise add a lightweight per-week selection or action entry point that makes the targeted week explicit. Preserve existing 4-week navigation behavior, but stop coupling AI affordances to `visibleWeeks[0]`.</action>
  <verify>Confirm that opening `Ukeplan` on week 2, 3, or 4 of the visible window still shows an AI setup/generation path for that week and does not require jumping another 4 weeks. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The page exposes AI planning for each visible week instead of only the first week of the current window.</done>
</task>

<task id="10-03-02" type="auto">
  <name>Make the selected visible week the single AI planning source of truth</name>
  <files>src/pages/WeeklyPlanPage.jsx</files>
  <action>Ensure the AI setup summary, recommendation context usage, and Replace-with-AI action all read from the same selected visible week object. Remove any remaining display-vs-generation divergence where the setup card can show one week but generation uses another. Keep compatibility with the selected-week handoff from `Treningsplan` so incoming non-current weeks still open in the correct state.</action>
  <verify>Inspect the generated invoke payload path and confirm the selected week dates match the visible week that the user chose for AI planning. Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The same selected week drives the setup card and the generation call.</done>
</task>

<task id="10-03-03" type="auto">
  <name>Add a regression for AI generation from a non-first visible week</name>
  <files>tests/unit/weeklyplan.test.jsx</files>
  <action>Add a focused test that renders a 4-week window, targets a visible week that is not the first item, and proves the page keeps AI setup available and sends the non-first week's dates through the Replace-with-AI flow. Include a handoff-driven case if needed to prove the page does not regress back to first-week anchoring.</action>
  <verify>Run `npm test -- --run tests/unit/weeklyplan.test.jsx`.</verify>
  <done>The first diagnosed UAT gap is protected by a page-level regression test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AI planning is reachable for any visible week in the current 4-week `Ukeplan` window
- [ ] Selected visible week drives both the AI setup summary and the Replace-with-AI request
- [ ] Non-first visible week no longer falls back to `visibleWeeks[0]`
- [ ] `npm test -- --run tests/unit/weeklyplan.test.jsx` passes
</verification>

<success_criteria>

- All tasks completed
- User can plan week 1, 2, 3, or 4 in the visible `Ukeplan` window with AI
- The selected visible week remains authoritative through setup and generation
- Regression coverage catches future first-week-only behavior

</success_criteria>

<output>
After completion, create `.planning/phases/10-recommendation-context/10-03-SUMMARY.md`
</output>
