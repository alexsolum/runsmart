---
phase: 02-replan-coach-context
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gemini-coach/index.ts
  - supabase/functions/gemini-coach/playbook.ts
  - tests/integration/edge-functions.spec.ts
autonomous: true
requirements:
  - RPLN-01
  - RPLN-02
  - PHIL-03
user_setup: []
must_haves:
  truths:
    - "Manual replanning returns week-level structure from current week through goal race date."
    - "Long-horizon mode still uses latest context payload signals and active runtime philosophy."
    - "Response remains schema-safe with deterministic fallback when model output is incomplete."
  artifacts:
    - path: "supabase/functions/gemini-coach/index.ts"
      provides: "Long-horizon replan mode with validated week-level contract through race date"
      exports: ["Deno.serve"]
    - path: "tests/integration/edge-functions.spec.ts"
      provides: "Integration coverage for long-horizon output shape and horizon boundary behavior"
      min_lines: 40
  key_links:
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "coach_philosophy_documents"
      via: "Runtime philosophy injection for long-term replan mode"
      pattern: "coach_philosophy_documents"
    - from: "supabase/functions/gemini-coach/index.ts"
      to: "goal race date in plan context"
      via: "Week-range generation bounded by race date"
      pattern: "goalDate|goal_race"
---

<objective>
Close the RPLN-01 horizon gap by introducing a long-term replanning contract that returns a weekly structure up to goal race.

Purpose: Existing `structured_plan` is fixed at 7 days and only partially satisfies requirement intent.
Output: A long-horizon coach mode with schema validation and philosophy-aware prompting that remains safe for downstream consumers.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replan-coach-context/02-RESEARCH.md
@.planning/phases/02-replan-coach-context/02-VERIFICATION.md
@CLAUDE.md
@supabase/functions/gemini-coach/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add long-horizon replan mode contract in edge function</name>
  <files>supabase/functions/gemini-coach/index.ts</files>
  <action>Introduce a dedicated replanning mode (for example `long_term_replan`) that returns an array of week objects covering current planning week through goal race date. Define explicit schema and enforce upper/lower bounds to prevent malformed or unbounded responses.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Edge function exposes a stable long-horizon output contract tied to race-date horizon.</done>
</task>

<task type="auto">
  <name>Task 2: Keep philosophy and safety precedence explicit for long-horizon mode</name>
  <files>supabase/functions/gemini-coach/index.ts, supabase/functions/gemini-coach/playbook.ts</files>
  <action>Apply active philosophy injection for the new mode while preserving instruction order: schema and safety guardrails first, then philosophy guidance, then fallback playbook context. Ensure parse/sanitize logic rejects invalid week items and returns safe fallback output.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Long-horizon mode is philosophy-aware without weakening schema safety.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for horizon-to-race behavior</name>
  <files>tests/integration/edge-functions.spec.ts</files>
  <action>Add integration assertions that long-term replan mode yields week-level entries extending to or covering the goal-race week, and validates behavior for missing/invalid race context fallback.</action>
  <verify><automated>npm run test:integration -- tests/integration/edge-functions.spec.ts</automated></verify>
  <done>Tests enforce requirement-level horizon coverage and output contract stability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Long-term replan mode exists and returns week-level horizon output to goal race
- [ ] Instruction precedence remains schema/safety first, philosophy second
- [ ] Invalid model output is sanitized/fallback-safe
- [ ] `npm run test:integration -- tests/integration/edge-functions.spec.ts` passes
</verification>

<success_criteria>

- `RPLN-01` long-horizon contract gap is closed at API level
- `PHIL-03` remains true for long-horizon replanning mode
- Output contract is deterministic and safe for UI consumption

</success_criteria>

<output>
After completion, create `.planning/phases/02-replan-coach-context/02-04-SUMMARY.md`
</output>
