---
phase: 02-replan-coach-context
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/coachPayload.js
  - src/pages/CoachPage.jsx
  - src/context/AppDataContext.jsx
  - tests/unit/coach.test.jsx
autonomous: true
requirements:
  - RPLN-02
  - PHIL-03
user_setup: []
must_haves:
  truths:
    - "Manual replanning payload always includes fresh activities, daily logs, and latest check-in."
    - "Check-in fields are normalized so edge prompts can reliably consume sleep/fatigue/recovery signals."
  artifacts:
    - path: "src/lib/coachPayload.js"
      provides: "Shared payload builder with explicit refresh and normalization"
      exports: ["buildCoachPayload"]
    - path: "src/pages/CoachPage.jsx"
      provides: "Coach page uses shared payload builder instead of local payload assembly"
      contains: "buildCoachPayload"
    - path: "tests/unit/coach.test.jsx"
      provides: "Unit coverage for payload freshness and check-in normalization"
      min_lines: 30
  key_links:
    - from: "src/pages/CoachPage.jsx"
      to: "src/lib/coachPayload.js"
      via: "shared payload builder import/use"
      pattern: "buildCoachPayload"
    - from: "src/lib/coachPayload.js"
      to: "activities|dailyLogs|checkins loaders"
      via: "invocation-time refresh before payload return"
      pattern: "loadActivities|loadLogs|loadCheckins"
---

<objective>
Create a shared and fresh context payload path for coach/replan invocations.

Purpose: Eliminate stale context risk and make input contract reusable by both Coach and Long Term plan flows.
Output: Shared payload module that refreshes required sources and normalizes check-in shape before edge invocation.
</objective>

<execution_context>
@C:/Users/evens/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evens/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replan-coach-context/02-RESEARCH.md
@.planning/phases/02-replan-coach-context/02-VALIDATION.md
@CLAUDE.md
@src/pages/CoachPage.jsx
@src/context/AppDataContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared coach payload module with refresh-first behavior</name>
  <files>src/lib/coachPayload.js</files>
  <action>Create `buildCoachPayload` that explicitly refreshes `activities`, `dailyLogs`, and `checkins` before constructing payload. Keep function deterministic and parameterized by current plan context and mode inputs.</action>
  <verify><automated>npm run test -- tests/unit/coach.test.jsx</automated></verify>
  <done>Shared helper exists and guarantees invocation-time freshness of required context data.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize check-in field shape for edge compatibility</name>
  <files>src/lib/coachPayload.js, src/context/AppDataContext.jsx</files>
  <action>Add normalization layer that accepts snake_case DB rows and emits stable camelCase values expected by coach prompt contract while retaining fallback compatibility fields when needed.</action>
  <verify><automated>npm run test -- tests/unit/coach.test.jsx</automated></verify>
  <done>Latest check-in context is stable and semantically complete for coach/replan prompts.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Coach page local payload logic and add unit tests</name>
  <files>src/pages/CoachPage.jsx, tests/unit/coach.test.jsx</files>
  <action>Remove/replace local payload construction in `CoachPage` with shared helper. Add tests proving required refresh functions are called and payload includes latest sources plus normalized check-in fields.</action>
  <verify><automated>npm run test -- tests/unit/coach.test.jsx</automated></verify>
  <done>Coach page uses one canonical payload path with unit coverage for freshness and field-shape guarantees.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Shared payload builder explicitly reloads activities, daily logs, and check-ins
- [ ] Latest check-in normalization covers snake_case/camelCase compatibility
- [ ] Coach page no longer maintains divergent payload logic
- [ ] `npm run test -- tests/unit/coach.test.jsx` passes
</verification>

<success_criteria>

- RPLN-02 payload freshness contract is implemented in shared code
- Replan-related context quality improves through consistent normalization
- Coach and Long Term flows can reuse one payload construction path

</success_criteria>

<output>
After completion, create `.planning/phases/02-replan-coach-context/02-02-SUMMARY.md`
</output>
